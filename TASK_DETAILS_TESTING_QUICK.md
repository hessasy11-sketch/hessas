# 🧪 صفحة تفاصيل المهمة - اختبار سريع
## Task Details Page - Quick Testing Guide

---

## ⚡ اختبار 5 دقائق

### الخطوة 1: فتح المهمة من My Work

```bash
1. سجل دخول كموظف
2. انتقل إلى /admin/my-work
3. اضغط على أي مهمة في القائمة
4. تحقق:
   ✅ انتقلت إلى /admin/tasks/staff/xxx أو /admin/tasks/farm/xxx
   ✅ الصفحة تفتح بدون أخطاء
   ✅ Header يظهر بشكل صحيح
```

**النتيجة:** إذا فتحت = ✅ نجاح

---

### الخطوة 2: التحقق من التفاصيل

```bash
تحقق من الأقسام التالية:

✅ Header:
   - عنوان المهمة
   - badge نوع المهمة (staff أو farm)
   - اسم المزرعة (إذا farm)
   - الحالة + الأولوية + الموعد

✅ القسم الأيسر:
   - الوصف
   - تفاصيل الإسناد (مسندة إلى، مسندة بواسطة)
   - الإثبات (إذا requires_proof)

✅ القسم الأيمن:
   - الإجراءات (بدء/إنهاء)
   - السجل الزمني
```

**النتيجة:** إذا كل الأقسام تظهر = ✅ نجاح

---

### الخطوة 3: اختبر أزرار العمل

#### إذا المهمة pending:

```bash
1. زر "▶️ بدء المهمة" يظهر في قسم الإجراءات
2. اضغط عليه
3. تحقق:
   ✅ الحالة تحولت إلى in_progress
   ✅ Timeline أضاف حدث: "بدأ العمل على المهمة"
   ✅ زر "✅ إنهاء المهمة" ظهر بدلاً من "بدء"
```

**النتيجة:** إذا الزر يعمل = ✅ نجاح

---

#### إذا المهمة in_progress:

```bash
1. زر "✅ إنهاء المهمة" يظهر
2. اضغط عليه
3. تحقق:
   ✅ إذا requires_proof ولا يوجد proof_url:
      - Modal يظهر: "رفع إثبات"
      - لا يتم إنهاء المهمة

   ✅ إذا لا يحتاج إثبات (أو موجود):
      - الحالة تحولت إلى under_review أو awaiting_approval
      - Timeline أضاف حدث: "تم إنهاء المهمة"
```

**النتيجة:** إذا الزر يعمل = ✅ نجاح

---

### الخطوة 4: اختبر رفع الإثبات

```bash
# فقط إذا requires_proof = true

1. في قسم "الإثبات":
   ✅ رسالة: "⚠️ لم يتم رفع الإثبات بعد"
   ✅ زر "رفع إثبات" موجود

2. اضغط على "رفع إثبات"
3. Modal يظهر مع حقل إدخال رابط
4. أدخل أي رابط: https://example.com/proof.jpg
5. اضغط "رفع"
6. تحقق:
   ✅ Modal يُغلق
   ✅ قسم الإثبات يتحدث:
      - "✅ تم رفع الإثبات"
      - "🔗 عرض الإثبات" (رابط)
   ✅ Timeline يتحدث

7. الآن اضغط "إنهاء المهمة":
   ✅ يعمل مباشرة (لأن الإثبات موجود)
```

**النتيجة:** إذا رفع الإثبات يعمل = ✅ نجاح

---

### الخطوة 5: اختبر الاعتماد (كمدير)

```bash
# 1. سجل خروج
# 2. سجل دخول كمدير/مشرف

1. انتقل إلى /admin/my-work
2. في تبويب "بانتظار الاعتماد"
3. اضغط على أي مهمة
4. تحقق:
   ✅ قسم "اعتماد المهمة" يظهر
   ✅ زر "✅ اعتماد" موجود
   ✅ زر "❌ رفض" موجود

5. اضغط على "اعتماد":
   ✅ تأكيد يظهر
   ✅ بعد التأكيد:
      - الحالة تحولت إلى completed
      - Timeline: "تم اعتماد المهمة"
```

**النتيجة:** إذا الاعتماد يعمل = ✅ نجاح

---

### الخطوة 6: اختبر الرفض (كمدير)

```bash
# على مهمة أخرى under_review

1. اضغط على زر "❌ رفض"
2. Modal يظهر: "رفض المهمة"
3. حقل textarea لإدخال السبب
4. حاول الإرسال بدون سبب:
   ✅ رسالة خطأ تظهر

5. أدخل سبب: "الصور غير واضحة"
6. اضغط "رفض"
7. تحقق:
   ✅ Modal يُغلق
   ✅ الحالة تحولت إلى rejected
   ✅ Timeline: "تم رفض المهمة"
   ✅ قسم "سبب الرفض" يظهر في الصفحة بلون أحمر
```

**النتيجة:** إذا الرفض يعمل = ✅ نجاح

---

### الخطوة 7: اختبر الصلاحيات

```bash
# اختبار الحماية

1. سجل دخول كموظف A (staff-123)
2. افتح مهمة الموظف A
   ✅ تفتح بشكل طبيعي

3. انسخ URL المهمة
4. سجل خروج
5. سجل دخول كموظف B (staff-456)
6. الصق URL المهمة (التي تخص الموظف A)
7. اضغط Enter
8. تحقق:
   ✅ لا تفتح المهمة
   ✅ يتم توجيهه إلى /admin/my-work
   ✅ رسالة: "حدث خطأ" أو "لم يتم العثور على المهمة"
```

**النتيجة:** إذا الصلاحيات تعمل = ✅ نجاح

---

### الخطوة 8: اختبر GM

```bash
# اختبار Bypass

1. سجل خروج
2. سجل دخول كـ General Manager
3. افتح أي مهمة (حتى لو ليست له)
4. تحقق:
   ✅ تفتح بشكل طبيعي
   ✅ canEdit = true (جميع الإجراءات متاحة)
   ✅ canApprove = true (الاعتماد/الرفض متاح)
```

**النتيجة:** إذا GM يرى كل شيء = ✅ نجاح

---

## 📊 النتائج السريعة

| الاختبار | النتيجة |
|----------|---------|
| فتح المهمة من My Work | ✅ / ❌ |
| التفاصيل تظهر بشكل صحيح | ✅ / ❌ |
| زر "بدء" يعمل | ✅ / ❌ |
| زر "إنهاء" يعمل | ✅ / ❌ |
| رفع الإثبات يعمل | ✅ / ❌ |
| الاعتماد يعمل (للمدير) | ✅ / ❌ |
| الرفض يعمل (للمدير) | ✅ / ❌ |
| الصلاحيات محكمة | ✅ / ❌ |
| GM يرى كل شيء | ✅ / ❌ |
| Timeline يتحدث | ✅ / ❌ |

---

## 🐛 مشاكل محتملة وحلول

### المشكلة 1: "لم يتم العثور على المهمة"

**السبب المحتمل:**
- taskId غير صحيح
- الموظف ليس له صلاحية

**الحل:**
```javascript
// افتح Console (F12)
// تحقق من URL
console.log(window.location.pathname);

// تحقق من session
const session = localStorage.getItem('staff_session');
console.log('Session:', JSON.parse(session));

// تحقق من المهمة في DB
SELECT * FROM staff_tasks WHERE id = 'task-id';
-- أو
SELECT * FROM farm_tasks WHERE id = 'task-id';
```

---

### المشكلة 2: "زر بدء/إنهاء لا يعمل"

**السبب المحتمل:**
- canEdit = false
- خطأ في التحديث

**الحل:**
```javascript
// افتح Console (F12)
// تحقق من:
console.log('canEdit:', canEdit);
console.log('canApprove:', canApprove);

// إذا canEdit = false:
// - تأكد أن assigned_to = currentStaffId
// - أو أن role = 'general_manager'

// إذا خطأ في التحديث:
// - تحقق من RLS policies
// - تحقق من الـ error في Console
```

---

### المشكلة 3: "Modal رفع الإثبات لا يظهر"

**السبب المحتمل:**
- requires_proof = false
- أو proof_url موجود بالفعل

**الحل:**
```javascript
// تحقق من:
console.log('requires_proof:', task.requires_proof);
console.log('proof_url:', task.proof_url);

// إذا requires_proof = false:
// - Modal لن يظهر (طبيعي)
// - إنهاء المهمة يعمل مباشرة
```

---

### المشكلة 4: "قسم الاعتماد لا يظهر"

**السبب المحتمل:**
- canApprove = false
- أو الحالة ليست under_review/awaiting_approval

**الحل:**
```javascript
// تحقق من:
console.log('canApprove:', canApprove);
console.log('status:', task.status);

// canApprove يكون true إذا:
// - role = 'general_manager'
// - أو role في ['supervisor', 'manager', 'farm_manager', 'accountant']
// - أو assigned_by = currentStaffId
```

---

### المشكلة 5: "Timeline فارغ"

**السبب المحتمل:**
- لا توجد أحداث بعد
- أو خطأ في fetchTimeline

**الحل:**
```javascript
// Timeline يُبنى من:
// - created_at (دائماً)
// - started_at (إذا موجود)
// - completed_at (إذا موجود)
// - approved_at (إذا موجود)
// - rejected_at (إذا موجود)

// إذا المهمة جديدة (pending):
// - سيظهر حدث واحد فقط: "تم إنشاء المهمة"
```

---

## ✅ قائمة تحقق سريعة

قبل إنهاء الاختبار:

- [ ] فتح المهمة من My Work يعمل
- [ ] URL صحيح: /admin/tasks/:type/:id
- [ ] badge نوع المهمة يظهر
- [ ] اسم المزرعة يظهر (للمزارع)
- [ ] التفاصيل كاملة
- [ ] زر "بدء" يعمل
- [ ] زر "إنهاء" يعمل
- [ ] Modal رفع الإثبات يعمل
- [ ] زر "اعتماد" يعمل (للمدير)
- [ ] Modal "رفض" يعمل (للمدير)
- [ ] Timeline يتحدث
- [ ] الصلاحيات محكمة
- [ ] زر "عودة" يرجع إلى My Work

---

## 🚀 النتيجة النهائية

إذا نجحت جميع الاختبارات:

```
✅ صفحة تفاصيل المهمة نشطة وجاهزة
✅ تعمل مع staff_tasks + farm_tasks
✅ الإجراءات (بدء، إنهاء، رفع إثبات، اعتماد، رفض) تعمل
✅ Timeline يتحدث مع كل إجراء
✅ Modals تعمل بشكل صحيح
✅ الصلاحيات محكمة (GM bypass)
✅ التكامل مع My Work Page يعمل

النظام جاهز للإنتاج! 🚀
```

---

**وقت الاختبار:** 5-7 دقائق
**الصعوبة:** متوسط
**الأهمية:** عالية جداً

ابدأ الاختبار الآن! 🧪
