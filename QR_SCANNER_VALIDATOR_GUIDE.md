# دليل نظام مسح وفحص QR Codes

## نظام شامل للتحقق من صحة QR Codes

### تم الإضافة ✅

1. **ماسح QR مدمج**
   - مسح مباشر من الكاميرا
   - فحص فوري للحالة
   - عرض تفصيلي للمشاكل

2. **إحصائيات متقدمة**
   - عدد الموظفين الكلي
   - QR نشط/معطّل
   - QR مكررة
   - موظفين بدون QR
   - نسبة الصحة (Health Score)

3. **كشف المشاكل التلقائي**
   - QR مكررة
   - QR معطّلة
   - موظفين معطّلين
   - QR مفقودة

4. **إصلاح تلقائي**
   - زر لإصلاح جميع التكرارات
   - توليد QR جديدة للمكررات
   - حفظ البيانات بأمان

## كيفية الوصول

### من مركز إدارة العمل:
```
Work Management Hub → بطاقات الموظفين → مسح وفحص QR
```

## الميزات المتوفرة

### 1. لوحة الإحصائيات
عرض فوري لـ:
- **إجمالي الموظفين**: العدد الكلي للموظفين النشطين
- **QR نشط**: عدد الرموز المفعّلة والجاهزة
- **QR معطّل**: رموز تحتاج تفعيل
- **QR مكرر**: رموز مستخدمة لأكثر من موظف (خطر!)
- **بدون QR**: موظفين بحاجة لتوليد QR
- **نسبة الصحة**: مؤشر عام على حالة النظام (0-100%)

### 2. ماسح QR
**كيفية الاستخدام:**
1. اضغط "ابدأ المسح"
2. اسمح للمتصفح بالوصول للكاميرا
3. وجّه الكاميرا نحو QR Code
4. انتظر المسح التلقائي

**النتيجة:**
- ✅ **QR صحيح**: أخضر - الرمز صالح وفعال
- ⚠️ **تحذيرات**: أصفر - مشاكل بسيطة (معطّل، بحاجة صيانة)
- ❌ **خطأ حرج**: أحمر - مكرر أو غير صحيح

### 3. عرض المشاكل
عند مسح QR، سترى:
- **حالة الرمز**: صحيح/مكرر/معطّل/غير موجود
- **عدد الموظفين**: كم موظف يستخدم نفس الرمز
- **قائمة المشاكل**:
  - مكرر (critical)
  - معطّل (warning)
  - موظف معطّل (warning)
- **معلومات الموظف**:
  - الاسم
  - رقم الموظف
  - القسم والدور
  - حالة التفعيل

### 4. قائمة QR المكررة
**عرض جميع التكرارات:**
- عدد التكرار لكل رمز
- أسماء الموظفين المتأثرين
- مؤشر الخطورة

**زر الإصلاح التلقائي:**
- يصلح جميع التكرارات دفعة واحدة
- يحتفظ بالموظف الأول
- يولد رموز جديدة للباقي
- آمن 100%

## Database Functions

### للإحصائيات
```sql
get_qr_codes_statistics()
-- يرجع: total_staff, active_qr, inactive_qr, duplicate_qr, missing_qr, health_score
```

### للبحث عن التكرارات
```sql
find_duplicate_qr_codes()
-- يرجع: qr_code, duplicate_count, staff_ids[], staff_names[]
```

### للفحص التفصيلي
```sql
check_qr_code_status(qr_code)
-- يرجع: valid, status, staff_count, staff_records, issues, message
```

### للمسح والحصول على المعلومات
```sql
scan_qr_and_get_info(qr_code)
-- يرجع: success, qr_status, permissions, scanned_at
```

### للإصلاح التلقائي
```sql
fix_duplicate_qr_codes()
-- يصلح جميع التكرارات ويرجع: success, fixed_count, message
```

## حالات QR Code

### ✅ صحيح ونشط (Valid & Active)
- QR موجود وفريد
- مفعّل للاستخدام
- الموظف نشط
- لديه صلاحيات

### ⚠️ معطّل (Inactive)
- QR موجود لكن معطّل
- يحتاج تفعيل من المشرف
- الموظف قد يكون نشط

### ❌ مكرر (Duplicate)
- **خطر أمني!**
- نفس QR لأكثر من موظف
- يجب الإصلاح فوراً
- قد يسبب مشاكل في الدخول

### ❓ غير موجود (Not Found)
- QR غير مسجل في النظام
- قد يكون مزور
- أو تم حذفه

## السيناريوهات الشائعة

### السيناريو 1: QR مكرر
**المشكلة**: موظفان لديهما نفس QR

**الحل:**
1. اذهب إلى تبويب "مسح وفحص QR"
2. انظر قائمة "QR مكررة"
3. اضغط "إصلاح الكل"
4. سيتم توليد QR جديد للموظف الثاني تلقائياً

### السيناريو 2: موظف بدون QR
**المشكلة**: موظف جديد ليس لديه QR

**الحل:**
- النظام يولد QR تلقائياً عند إنشاء موظف جديد
- إذا كان قديم بدون QR، استخدم:
```sql
SELECT generate_qr_for_staff('staff_id');
```

### السيناريو 3: QR لا يعمل عند الدخول
**التشخيص:**
1. امسح QR في النظام
2. تحقق من:
   - هل QR مفعّل؟
   - هل الموظف نشط؟
   - هل لديه صلاحيات؟
   - هل QR مكرر؟

**الحل حسب المشكلة:**
- معطّل → فعّله من القائمة
- مكرر → أصلح التكرار
- بدون صلاحيات → أضف Permission Pack

## الأمان والتدقيق

### تسجيل العمليات
كل عملية مسح تُسجل في `audit_logs`:
```json
{
  "action": "scan_qr",
  "entity_type": "qr_code",
  "details": {
    "qr_code": "QR-XXXXX",
    "status": "duplicate",
    "scanned_at": "timestamp"
  }
}
```

### الصلاحيات
- فقط المشرفون يمكنهم:
  - مسح وفحص QR
  - رؤية التكرارات
  - إصلاح المشاكل
  - توليد QR جديدة

## نسبة الصحة (Health Score)

يتم حسابها كالتالي:
```
Health Score = (QR نشط / إجمالي الموظفين) × 100
```

**التفسير:**
- **90-100%**: ممتاز ✅
- **70-89%**: جيد ⚠️
- **50-69%**: يحتاج انتباه ⚠️
- **أقل من 50%**: حرج ❌

## أفضل الممارسات

1. **فحص دوري**
   - افحص نسبة الصحة يومياً
   - ابحث عن تكرارات أسبوعياً

2. **إصلاح فوري**
   - أصلح التكرارات فور اكتشافها
   - لا تترك QR معطّلة بدون سبب

3. **توثيق**
   - سجّل سبب التعطيل
   - احتفظ بسجل التغييرات

4. **تدريب**
   - درّب الموظفين على استخدام QR
   - علّمهم الإبلاغ عن المشاكل

## استكشاف الأخطاء

### المسح لا يعمل
- **السبب**: الكاميرا غير متاحة
- **الحل**: اسمح للمتصفح بالوصول للكاميرا

### QR لا يُقرأ
- **السبب**: جودة الطباعة سيئة
- **الحل**: أعد طباعة البطاقة بجودة أعلى

### "فشل مسح QR Code"
- **السبب**: QR تالف أو غير صحيح
- **الحل**: ولّد QR جديد للموظف

## الدعم الفني

عند مواجهة مشكلة:
1. تحقق من نسبة الصحة
2. افحص قائمة التكرارات
3. امسح QR يدوياً للتشخيص
4. راجع Audit Logs
5. استخدم الإصلاح التلقائي إذا لزم

## ملاحظات مهمة

⚠️ **تحذيرات:**
- لا تشارك QR أبداً
- لا تترك تكرارات بدون إصلاح
- راقب نسبة الصحة دائماً
- أصلح المشاكل فوراً

✅ **أفضل طريقة:**
- فحص يومي
- إصلاح تلقائي
- توثيق واضح
- تدريب مستمر
