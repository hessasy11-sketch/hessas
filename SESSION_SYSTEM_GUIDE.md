# نظام الجلسات المتقدم - دليل الاستخدام

## نظرة عامة
تم تفعيل نظام جلسات متقدم يحمي لوحة التحكم من الوصول غير المصرح به ويضمن أمان البيانات.

## المزايا الرئيسية

### 1. مدة الجلسة
- **المدة الأساسية**: 60 دقيقة من عدم النشاط
- **التجديد التلقائي**: يتم تجديد الجلسة تلقائياً عند أي نشاط (نقر، كتابة، حركة الماوس، إلخ)
- **التحديث الذكي**: يتم تحديث الجلسة كل 5 ثوان فقط لتحسين الأداء

### 2. التحذيرات والإشعارات
- **تحذير مبكر**: عند تبقي 10 دقائق أو أقل، يظهر إشعار تحذيري برتقالي في أعلى الشاشة
- **العداد المباشر**: يعرض عدد الدقائق المتبقية بدقة
- **إمكانية الخروج الفوري**: زر "تسجيل خروج الآن" متاح في التحذير

### 3. الخروج التلقائي
- عند انتهاء مدة الجلسة (60 دقيقة بدون نشاط)
- يتم إعادة التوجيه تلقائياً لصفحة الدخول
- مسح كامل لبيانات الجلسة من المتصفح

### 4. الخروج الصريح
- **زر الخروج**: متاح في جميع الصفحات
  - في لوحة B2F: زر "خروج" أحمر بارز في الأعلى
  - في HQ Dashboard: زر "خروج" في الـ header
  - في Command Center: زر "خروج" واضح
- **الخروج الفوري**: مسح فوري للجلسة والتوجيه لصفحة الدخول

## كيفية عمل النظام

### عند الدخول بنجاح (QR/PIN)
```typescript
adminSessionManager.createSession({
  staff_id: "...",
  user_id: "...",
  full_name: "...",
  role: "...",
  // ... بقية البيانات
});
```

### تتبع النشاط
- يتم تتبع الأحداث التالية:
  - النقر (click)
  - الكتابة (keydown)
  - التمرير (scroll)
  - حركة الماوس (mousemove)
  - اللمس (touchstart)

### الفحص الدوري
- كل 10 ثوان يتم فحص:
  - الوقت المتبقي للجلسة
  - إذا انتهت المدة → خروج تلقائي
  - إذا تبقى 10 دقائق أو أقل → إظهار التحذير

## الصفحات المحمية

### ✅ الصفحات المفعّل فيها نظام الجلسات:
1. **HQ Dashboard** (`/hq`)
2. **B2F Control Panel** (`/admin/b2f`)
3. **Auctions Management** (`/admin/auctions`)
4. **Settings Admin** (`/admin/settings`)
5. **Platform Command Center**

## الإعدادات المتقدمة

### تغيير مدة الجلسة
في ملف `src/utils/adminSessionManager.ts`:
```typescript
const IDLE_TIMEOUT_MS = 60 * 60 * 1000; // 60 دقيقة
```

يمكن تغيير القيمة حسب الحاجة:
- `30 * 60 * 1000` = 30 دقيقة
- `120 * 60 * 1000` = 120 دقيقة (ساعتان)

### تغيير وقت التحذير
في ملف `src/components/platform/SessionTracker.tsx`:
```typescript
} else if (minutes <= 10) {  // التحذير عند 10 دقائق
  setShowWarning(true);
}
```

### تغيير تكرار الفحص
في ملف `src/components/platform/SessionTracker.tsx`:
```typescript
const checkInterval = setInterval(() => {
  // ...
}, 10000);  // 10 ثوان
```

## معلومات الجلسة المخزنة

يتم تخزين البيانات التالية في `localStorage`:
```typescript
{
  staff_id: string;
  user_id: string;
  full_name: string;
  role: string;
  role_title: string;
  department: string;
  is_super_admin: boolean;
  is_platform_owner: boolean;
  created_at: number;
  last_activity_at: number;
  current_farm_id?: string | null;
  available_farms?: FarmContext[];
  farm_roles_map?: Record<string, string>;
}
```

## API Functions

### إنشاء جلسة
```typescript
adminSessionManager.createSession(sessionData);
```

### الحصول على الجلسة
```typescript
const session = adminSessionManager.getSession();
```

### تحديث النشاط
```typescript
adminSessionManager.updateActivity();
```

### إنهاء الجلسة
```typescript
adminSessionManager.destroySession();
```

### التحقق من المصادقة
```typescript
const isAuth = adminSessionManager.isAuthenticated();
```

### الحصول على الوقت المتبقي
```typescript
const minutes = adminSessionManager.getRemainingMinutes();
```

## الأمان

### الحماية الآلية
- ✅ مسح تلقائي للجلسة عند انتهاء المدة
- ✅ فحص دوري لصلاحية الجلسة
- ✅ حماية من الجلسات المنتهية
- ✅ تشفير وتخزين آمن في localStorage

### أفضل الممارسات
1. **لا تشارك QR Code الخاص بك**
2. **سجل خروجك دائماً عند الانتهاء**
3. **انتبه للتحذيرات عند اقتراب انتهاء الجلسة**
4. **قم بأي نشاط لتجديد الجلسة إذا كنت بحاجة للمزيد من الوقت**

## استكشاف الأخطاء

### المشكلة: الخروج المفاجئ
**الحل**: تأكد من النشاط المستمر على الصفحة

### المشكلة: عدم ظهور التحذير
**الحل**: تحقق من أن `SessionTracker` مفعّل في الصفحة

### المشكلة: الجلسة لا تتجدد
**الحل**: تأكد من تفعيل `initActivityTracking()`

## ملاحظات مهمة

1. **الجلسة محلية**: يتم تخزين الجلسة في متصفحك فقط
2. **الأمان أولاً**: لا تشارك جهازك مع أشخاص آخرين أثناء تسجيل دخولك
3. **التحديثات التلقائية**: النظام يحدث نفسه تلقائياً دون الحاجة لتدخل
4. **التوافق**: يعمل على جميع المتصفحات الحديثة

---

## التحديثات الأخيرة (2026-01-04)

✅ زيادة مدة الجلسة من 30 إلى 60 دقيقة
✅ تحسين التحذيرات (10 دقائق بدلاً من 5)
✅ إضافة زر "خروج" واضح وبارز في جميع الصفحات
✅ تحسين تجربة المستخدم على الهاتف والكمبيوتر
✅ إصلاح مشكلة الخروج في B2F Control Panel

---

**نظام الجلسات يعمل الآن بكفاءة عالية!**
