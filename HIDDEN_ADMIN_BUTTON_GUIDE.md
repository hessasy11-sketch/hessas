# دليل زر الدخول الإداري المخفي

**الإصدار:** 1.0.0
**التاريخ:** 3 يناير 2026
**الحالة:** ✅ فعال

---

## نظرة عامة

زر دخول إداري مخفي في الواجهة الرئيسية للتطبيق يوفر مدخلاً موحداً لجميع أعضاء الإدارة (مدير عام، مديرين، مشرفين).

### الفلسفة الأساسية

```
زر واحد مخفي
    ↓
يدخل منه كل من له صلاحية
    ↓
النظام هو من يقرر أين يذهب
```

---

## الوصف

### الشكل والمظهر

**شكل الزر:**
- تاج دائري صغير (Crown Icon)
- بدون نص
- شبه شفاف في الحالة الطبيعية
- لا يظهر كزر تقليدي

**الموقع:**
- في Header (الرأس)
- بجانب زر اللغة
- في الزاوية العلوية اليسرى

**الحالات البصرية:**

1. **الحالة الافتراضية** (0 ضغطات)
   ```
   - اللون: أبيض شفاف (30% opacity)
   - الحجم: صغير (14px)
   - الخلفية: شفافة تماماً
   - التأثير: لا يوجد
   ```

2. **أثناء التفعيل** (1-4 ضغطات)
   ```
   - اللون: ذهبي (#f59e0b)
   - الحجم: نفسه
   - الخلفية: gradient ذهبي خفيف
   - التأثير: توهج ذهبي متزايد
   - عداد: يظهر فوق الزر (مثلاً: 3/5)
   - Border: حد ذهبي شفاف
   ```

3. **التفعيل الكامل** (5 ضغطات)
   ```
   - اللون: ذهبي مشرق (#fbbf24)
   - الحجم: يكبر قليلاً (scale 1.3)
   - الخلفية: gradient ذهبي قوي
   - التأثير: دوران + توهج قوي
   - الانتقال: إلى بوابة الدخول الإداري
   ```

---

## آلية العمل

### متطلبات التفعيل

```typescript
MAX_TAPS = 5           // عدد الضغطات المطلوبة
TAP_TIMEOUT = 3000ms   // المدة القصوى بين الضغطات (3 ثواني)
```

### التدفق

```
المستخدم يضغط على التاج
    ↓
الضغطة الأولى → عداد: 1/5
    ↓
خلال 3 ثوانٍ → ضغطة ثانية → عداد: 2/5
    ↓
خلال 3 ثوانٍ → ضغطة ثالثة → عداد: 3/5
    ↓
خلال 3 ثوانٍ → ضغطة رابعة → عداد: 4/5
    ↓
خلال 3 ثوانٍ → ضغطة خامسة → عداد: 5/5
    ↓
تأثير بصري (دوران + توهج)
    ↓
انتقال إلى: /admin/access
    ↓
بوابة الدخول الإداري (AdminSmartAccessGateV2)
```

### حالات الفشل

**1. انتهاء الوقت:**
```
إذا مرت أكثر من 3 ثوانٍ بين ضغطة وأخرى
    ↓
العداد يُعاد إلى صفر
    ↓
الزر يعود لحالته الطبيعية
```

**2. ضغطة واحدة أو اثنتين:**
```
ضغطة عشوائية → لا يحدث شيء
الزر يختفي بعد 3 ثوانٍ
```

---

## التطبيق التقني

### الملفات

**`src/components/platform/HiddenAdminButton.tsx`**
```typescript
المكون الرئيسي للزر المخفي
- إدارة حالة الضغطات
- التحقق من الوقت
- التأثيرات البصرية
- الانتقال إلى /admin/access
```

**`src/components/Header.tsx`**
```typescript
يحتوي على HiddenAdminButton
موقعه: بجانب زر اللغة
```

**`src/App.tsx`**
```typescript
Route المسار:
/admin/access → AdminSmartAccessGateV2
```

### الكود الأساسي

```typescript
// تتبع الضغطات
const [taps, setTaps] = useState(0);
const [lastTapTime, setLastTapTime] = useState(0);

const handleTap = () => {
  const now = Date.now();

  // إذا مر وقت طويل، ابدأ من جديد
  if (now - lastTapTime > TAP_TIMEOUT) {
    setTaps(1);
  } else {
    setTaps(prev => prev + 1);
  }

  setLastTapTime(now);
};

// عند الوصول إلى 5 ضغطات
useEffect(() => {
  if (taps >= MAX_TAPS) {
    setTimeout(() => {
      navigate('/admin/access');
      setTaps(0);
    }, 500);
  }
}, [taps]);

// إعادة العداد تلقائياً
useEffect(() => {
  if (taps > 0 && taps < MAX_TAPS) {
    const timer = setTimeout(() => {
      setTaps(0);
    }, TAP_TIMEOUT);
    return () => clearTimeout(timer);
  }
}, [taps, lastTapTime]);
```

---

## رحلة المستخدم الإداري

### الخطوة 1: اكتشاف الزر

```
المستخدم الإداري يعرف وجود الزر
    ↓
يبحث عنه في Header
    ↓
يرى تاج صغير شبه شفاف
```

### الخطوة 2: التفعيل

```
يضغط 5 مرات متتالية خلال 3 ثوانٍ
    ↓
يرى العداد يتقدم: 1/5 → 2/5 → 3/5 → 4/5 → 5/5
    ↓
يرى تأثير الدوران والتوهج
```

### الخطوة 3: بوابة الدخول

```
ينتقل إلى: /admin/access
    ↓
يرى شاشة AdminSmartAccessGateV2
    ↓
خيارات:
  1. مسح QR بالكاميرا
  2. رفع صورة QR
  3. إدخال Token يدوياً
```

### الخطوة 4: التحقق

```
النظام يتحقق من QR Token
    ↓
إذا كان الموظف يحتاج PIN
    ↓
يُطلب منه إدخال PIN
    ↓
بعد التحقق الكامل
    ↓
التوجيه التلقائي حسب الدور
```

### الخطوة 5: التوجيه

```
Role: super_admin → /hq
Role: manager (B2F) → /b2f
Role: manager (B2B) → /companies
Role: supervisor → /b2f
Role: finance → /finance
Role: support → /support
Default → /dashboard
```

---

## الأمان

### مستويات الحماية

**المستوى 1: الزر المخفي**
```
- غير واضح للمستخدمين العاديين
- لا يوجد نص أو تلميح
- يتطلب معرفة مسبقة
```

**المستوى 2: التفعيل المتكرر**
```
- 5 ضغطات متتالية
- خلال 3 ثوانٍ
- منع الضغطات العشوائية
```

**المستوى 3: QR Token**
```
- كل موظف له QR فريد ومشفر
- QR Token يُولد عشوائياً
- طول 32 بايت مشفرة بـ base64
```

**المستوى 4: PIN (اختياري)**
```
- للأدوار الحساسة
- مشفر بـ bcrypt
- تتبع المحاولات الفاشلة
- قفل تلقائي بعد 5 محاولات
```

**المستوى 5: Device Fingerprinting**
```
- تسجيل معلومات الجهاز
- تتبع نقطة الدخول
- Audit log شامل
```

---

## حالات الاستخدام

### سيناريو 1: المدير العام (أول مرة)

```
1. يفتح التطبيق
2. يضغط على التاج 5 مرات
3. ينتقل إلى /admin/access
4. يرفع صورة الباركود المؤقت
5. يدخل PIN (123456)
6. ينتقل تلقائياً إلى /hq
7. يرى تنبيه "باركود مؤقت - يرجى استبداله"
8. يضغط "استبدال الباركود الآن"
9. يحصل على باركود دائم
✅ جاهز للاستخدام
```

### سيناريو 2: مشرف عادي

```
1. يفتح التطبيق
2. يضغط على التاج 5 مرات
3. ينتقل إلى /admin/access
4. يمسح QR بالكاميرا
5. يدخل PIN الخاص به
6. ينتقل تلقائياً إلى /b2f
✅ يبدأ العمل
```

### سيناريو 3: موظف دعم (بدون PIN)

```
1. يفتح التطبيق
2. يضغط على التاج 5 مرات
3. ينتقل إلى /admin/access
4. يمسح QR بالكاميرا
5. يدخل مباشرة (لا يُطلب PIN)
6. ينتقل تلقائياً إلى /support
✅ يبدأ العمل
```

---

## الاختلافات عن الأنظمة التقليدية

### الأنظمة التقليدية

```
❌ صفحة تسجيل دخول منفصلة
❌ حقل username
❌ حقل password
❌ زر "تسجيل الدخول"
❌ رابط "نسيت كلمة المرور"
```

### نظامنا

```
✅ زر واحد مخفي في الواجهة الرئيسية
✅ تفعيل بـ 5 ضغطات متتالية
✅ QR Code للتحقق
✅ PIN اختياري (حسب الدور)
✅ توجيه تلقائي ذكي
```

---

## الميزات الخاصة

### 1. تجربة مستخدم سلسة

```
- لا توجد صفحة تسجيل دخول منفصلة
- الزر جزء من التصميم العام
- التأثيرات البصرية جذابة
- العداد يعطي feedback فوري
```

### 2. أمان متعدد الطبقات

```
- الزر مخفي (معرفة مسبقة)
- 5 ضغطات متتالية (منع عشوائي)
- QR Token فريد (تشفير)
- PIN اختياري (للأدوار الحساسة)
- Device tracking (تتبع)
```

### 3. ذكاء التوجيه

```
- النظام يعرف دور المستخدم
- يوجهه تلقائياً لقسمه
- لا حاجة للتنقل اليدوي
- تجربة مخصصة لكل دور
```

### 4. مرونة الصلاحيات

```
- الزر لا يمنح صلاحيات
- الصلاحيات تُحدد من database
- النظام هو من يقرر
- سهولة إضافة/إزالة موظفين
```

---

## التوثيق الفني

### Props

```typescript
// HiddenAdminButton لا يستقبل Props
// يعمل بشكل مستقل تماماً
```

### State

```typescript
interface State {
  taps: number;              // عدد الضغطات الحالي (0-5)
  lastTapTime: number;       // timestamp آخر ضغطة
  isAnimating: boolean;      // حالة التأثير البصري
}
```

### Constants

```typescript
const MAX_TAPS = 5;          // عدد الضغطات المطلوبة
const TAP_TIMEOUT = 3000;    // المدة القصوى بالمللي ثانية
```

### Effects

```typescript
// Effect 1: التحقق من اكتمال الضغطات
useEffect(() => {
  if (taps >= MAX_TAPS) {
    // تفعيل التأثير البصري
    // الانتقال إلى /admin/access
  }
}, [taps]);

// Effect 2: إعادة العداد تلقائياً
useEffect(() => {
  if (taps > 0 && taps < MAX_TAPS) {
    const timer = setTimeout(() => {
      setTaps(0);
    }, TAP_TIMEOUT);
    return () => clearTimeout(timer);
  }
}, [taps, lastTapTime]);
```

---

## التخصيص والإعدادات

### تغيير عدد الضغطات

```typescript
// في HiddenAdminButton.tsx
const MAX_TAPS = 5;  // غير هذا الرقم (مثلاً: 3 أو 7)
```

### تغيير المدة الزمنية

```typescript
// في HiddenAdminButton.tsx
const TAP_TIMEOUT = 3000;  // غير هذا الرقم بالمللي ثانية
// مثال: 5000 = 5 ثوانٍ
```

### تغيير الألوان

```typescript
// في HiddenAdminButton.tsx
// البحث عن:
color: 'rgba(255, 255, 255, 0.3)'  // اللون الافتراضي
color: '#f59e0b'                    // اللون عند التفعيل
color: '#fbbf24'                    // اللون عند الاكتمال
```

### تغيير حجم الزر

```typescript
// في HiddenAdminButton.tsx
className="w-7 h-7"  // غير هذا (مثلاً: w-8 h-8)
className="w-3.5 h-3.5"  // حجم الأيقونة (مثلاً: w-4 h-4)
```

---

## الاختبار

### اختبار يدوي

**الخطوات:**
```
1. افتح التطبيق في المتصفح
2. انظر إلى Header في الزاوية العلوية اليسرى
3. ابحث عن تاج صغير شبه شفاف
4. اضغط عليه 5 مرات بسرعة (خلال 3 ثوانٍ)
5. راقب:
   - العداد يظهر ويتقدم
   - الزر يتوهج تدريجياً
   - عند 5/5 يدور ويلمع
   - ينتقل إلى /admin/access
```

**النتيجة المتوقعة:**
```
✅ الزر مرئي لكن غير بارز
✅ العداد يظهر عند الضغط
✅ التأثيرات البصرية تعمل
✅ الانتقال يحدث بعد 5 ضغطات
✅ الصفحة /admin/access تظهر
```

### اختبار الفشل

**1. ضغطة واحدة:**
```
الخطوات:
1. اضغط على الزر مرة واحدة
2. انتظر 4 ثوانٍ

النتيجة المتوقعة:
✅ الزر يعود لحالته الطبيعية
✅ لا يحدث انتقال
```

**2. بطء في الضغطات:**
```
الخطوات:
1. اضغط على الزر
2. انتظر 4 ثوانٍ
3. اضغط مرة أخرى

النتيجة المتوقعة:
✅ العداد يُعاد من جديد (1/5)
✅ لا يحدث انتقال
```

---

## الدعم والمساعدة

### للمستخدمين الإداريين

```
سؤال: أين الزر؟
جواب: في Header، بجانب زر اللغة، على شكل تاج صغير

سؤال: كيف أستخدمه؟
جواب: اضغط 5 مرات بسرعة (خلال 3 ثوانٍ)

سؤال: ماذا بعد الضغط؟
جواب: ستنتقل إلى بوابة الدخول، امسح الباركود الخاص بك

سؤال: هل يعطيني صلاحيات؟
جواب: لا، الزر فقط للدخول. الصلاحيات تُحدد من حسابك
```

### للفريق التقني

```
سؤال: كيف أغير عدد الضغطات؟
جواب: عدّل MAX_TAPS في HiddenAdminButton.tsx

سؤال: كيف أغير الألوان؟
جواب: عدّل قيم الألوان في style props

سؤال: هل يمكن إخفاءه كلياً؟
جواب: نعم، احذف <HiddenAdminButton /> من Header.tsx

سؤال: هل يمكن نقله لمكان آخر؟
جواب: نعم، انسخ <HiddenAdminButton /> إلى أي مكون آخر
```

---

## الخلاصة

### ما تم إنجازه

```
✅ زر مخفي على شكل تاج دائري صغير
✅ لا نص، جزء طبيعي من التصميم
✅ يتطلب 5 ضغطات متتالية خلال 3 ثوانٍ
✅ تأثيرات بصرية جذابة وذكية
✅ انتقال سلس إلى بوابة الدخول الإداري
✅ مدخل موحد لجميع الإداريين
✅ النظام يقرر الصلاحيات والتوجيه
```

### الفلسفة النهائية

```
╔═══════════════════════════════════════════════════╗
║                                                   ║
║              زر واحد مخفي                        ║
║                     ↓                             ║
║        يدخل منه كل من له صلاحية                 ║
║                     ↓                             ║
║         النظام هو من يقرر أين يذهب              ║
║                                                   ║
╚═══════════════════════════════════════════════════╝
```

---

**تم بحمد الله**

**الفريق التقني - 3 يناير 2026**
