# دليل غرفة عمليات المزادات B2B - المرحلة D

## نظرة عامة

تم بناء غرفة عمليات المزادات (B2B) كاملة مع جميع المكونات المطلوبة: Pulse، Radar، Quick Actions، Decision Queue، و Executive Log.

---

## المسار

```
/admin/operations-room/b2b
```

---

## المكونات الرئيسية

### 1. Pulse (النبض الحي)

**الموقع:** أعلى الصفحة

**البيانات المعروضة:**
```typescript
{
  visits_today: number           // زيارات B2B اليوم
  active_auctions: number        // مزادات نشطة
  critical_auctions: number      // مزادات حرجة (أقل من 24 ساعة)
  highest_bid_today: number      // أعلى عرض اليوم (ريال)
}
```

**التحديث:** كل 30 ثانية تلقائياً

**الدالة:**
```sql
get_b2b_ops_pulse()
```

---

### 2. Radar - قائمة المزادات

**الموقع:** العمود الأيسر (الرئيسي)

**معلومات كل مزاد:**
- العنوان والفئة
- الحالة (نشط/متوقف/مكتمل/مباع/ملغي)
- السعر الحالي والابتدائي
- عدد المشاهدات
- عدد المزايدات
- أعلى عرض
- الوقت المتبقي
- حالة حرجة (أقل من 24 ساعة)
- اسم البائع

**ترتيب المزادات:**
1. مزادات حرجة (أقل من 24 ساعة) أولاً
2. مزادات نشطة
3. باقي الحالات

**تنبيه المزادات الحرجة:**
- حد أحمر على الجانب
- أيقونة تحذير متحركة
- لون أحمر للوقت المتبقي

**الدالة:**
```sql
get_b2b_auctions_radar()
```

---

### 3. Quick Actions (للمدير العام)

**الإجراءات المتاحة للمزادات النشطة:**

#### أ. إيقاف المزاد
```sql
exec_toggle_auction_status(
  p_auction_id: uuid,
  p_new_status: 'paused',
  p_performed_by: uuid,
  p_notes: text
)
```

#### ب. تمديد الوقت
```sql
exec_extend_auction_time(
  p_auction_id: uuid,
  p_hours_to_add: integer,
  p_performed_by: uuid,
  p_notes: text
)
```
- التمديد الافتراضي: 24 ساعة
- يمكن تكرار التمديد

#### ج. اعتماد النتيجة
```sql
exec_approve_auction_result(
  p_auction_id: uuid,
  p_performed_by: uuid,
  p_notes: text
)
```
- يظهر فقط إذا كان هناك مزايدات
- يحدد الفائز تلقائياً (أعلى عرض)
- يغير حالة المزاد إلى "مباع"

**الإجراءات المتاحة للمزادات المتوقفة:**

#### تفعيل المزاد
```sql
exec_toggle_auction_status(
  p_auction_id: uuid,
  p_new_status: 'active',
  p_performed_by: uuid,
  p_notes: text
)
```

**ميزة مهمة:** جميع الإجراءات تُسجل تلقائياً في Executive Log

---

### 4. Decision Queue (طابور القرارات)

**الموقع:** العمود الأيمن

**أنواع القرارات:**
1. إيقاف مزاد (`pause_auction`)
2. تمديد مزاد (`extend_auction`)
3. اعتماد نتيجة مزاد (`approve_auction_result`)
4. إلغاء مزاد (`cancel_auction`)
5. قرارات B2F (إذا كانت موجودة)

**مستويات الأولوية:**
- `urgent` - عاجل (أحمر)
- `high` - عالي (برتقالي)
- `normal` - عادي (أزرق)
- `low` - منخفض (رمادي)

**الحالات:**
- `pending` - معلق
- `approved` - موافق عليه
- `rejected` - مرفوض
- `executed` - منفذ

**الدالة:**
```sql
get_pending_decisions()
```

**دوال التنفيذ:**
```sql
-- الموافقة
exec_approve_decision(
  p_decision_id: uuid,
  p_approved_by: uuid,
  p_notes: text
)

-- الرفض
exec_reject_decision(
  p_decision_id: uuid,
  p_rejected_by: uuid,
  p_notes: text
)
```

---

### 5. Executive Log (السجل التنفيذي)

**الموقع:** أسفل العمود الأيسر

**الإجراءات المسجلة للمزادات:**
- `auction_activated` - تفعيل مزاد
- `auction_paused` - إيقاف مزاد
- `auction_cancelled` - إلغاء مزاد
- `auction_time_extended` - تمديد وقت مزاد
- `auction_result_approved` - اعتماد نتيجة مزاد

**الإجراءات المسجلة لـ B2F:**
- `farm_manager_assigned` - تعيين مدير
- `farm_manager_changed` - تغيير مدير
- `farm_activated` - تشغيل مزرعة
- `farm_paused` - إيقاف مزرعة
- `expense_approved` - اعتماد مصروف
- `bookings_toggled` - تغيير حالة الحجوزات

**معلومات كل سجل:**
- نوع الإجراء
- عنوان المزاد أو اسم المزرعة
- التفاصيل الإضافية:
  - ساعات التمديد
  - عرض الفوز
  - الحالة القديمة والجديدة
- من قام بالإجراء
- النتيجة (نجاح/فشل)
- التاريخ والوقت

**الدالة:**
```sql
get_executive_logs(limit_count: integer)
```

**التسجيل التلقائي:** كل إجراء يُسجل إلزامياً

---

## قاعدة البيانات

### استخدام الجداول الموجودة

تستخدم غرفة عمليات B2B نفس جداول Decision Queue و Executive Log:
- `decision_queue`
- `executive_logs`

### الدوال الجديدة

#### 1. get_b2b_ops_pulse()
```sql
-- يحسب:
-- زيارات اليوم (dummy حالياً)
-- مزادات نشطة (status = 'active' AND end_time > NOW())
-- مزادات حرجة (نشطة وتنتهي خلال 24 ساعة)
-- أعلى عرض اليوم
```

#### 2. get_b2b_auctions_radar()
```sql
-- يجلب:
-- جميع المزادات (active, pending, completed)
-- بيانات كاملة لكل مزاد
-- عدد المزايدات وأعلى عرض
-- الوقت المتبقي بالساعات
-- حالة حرجة (is_critical)
-- مرتبة حسب الأولوية والموعد
```

#### 3. exec_toggle_auction_status()
```sql
-- يغير حالة المزاد
-- الحالات المسموحة: active, paused, cancelled
-- يسجل في Executive Log تلقائياً
```

#### 4. exec_extend_auction_time()
```sql
-- يضيف ساعات للمزاد
-- يحدث end_time
-- يسجل في Executive Log تلقائياً
```

#### 5. exec_approve_auction_result()
```sql
-- يحدد الفائز (أعلى عرض)
-- يغير الحالة إلى 'sold'
-- يسجل في Executive Log تلقائياً
```

---

## الاستخدام

### 1. فتح غرفة العمليات
```
المسار: /admin/operations-room
→ اضغط على بطاقة "المزادات"
→ يفتح: /admin/operations-room/b2b
```

### 2. مراقبة النبض
- راقب الأرقام في الأعلى
- المزادات الحرجة تظهر بلون أحمر متحرك
- التحديث التلقائي كل 30 ثانية

### 3. إدارة المزادات
- استعرض قائمة المزادات في Radar
- المزادات الحرجة تظهر أولاً
- اضغط على المزاد لاختياره
- تظهر Quick Actions للمزادات النشطة

### 4. Quick Actions
**للمزادات النشطة:**
- إيقاف: يوقف المزاد مؤقتاً
- تمديد 24س: يضيف 24 ساعة
- اعتماد النتيجة: يحدد الفائز (إذا كانت هناك مزايدات)

**للمزادات المتوقفة:**
- تفعيل: يعيد تشغيل المزاد

### 5. معالجة القرارات
- راجع القرارات المعلقة
- افحص الأولوية والتفاصيل
- اضغط "موافقة" للتنفيذ
- اضغط "رفض" للرفض

### 6. مراجعة السجل
- راجع آخر الإجراءات
- تحقق من النتائج
- اقرأ التفاصيل الإضافية

---

## الأمان

### RLS Policies
- استخدام نفس policies لـ decision_queue و executive_logs
- Service role له صلاحيات كاملة
- المستخدمون المصادق عليهم لهم صلاحية قراءة فقط
- التنفيذ يتطلب مصادقة

### التسجيل الإلزامي
- كل إجراء يُسجل تلقائياً
- لا يمكن حذف السجلات
- معلومات كاملة لكل إجراء

---

## المزايا

### 1. الرؤية الشاملة
- Pulse حي بالأرقام الحرجة
- قائمة شاملة لجميع المزادات
- تنبيهات للمزادات الحرجة

### 2. التحكم السريع
- Quick Actions مباشرة
- إيقاف/تفعيل فوري
- تمديد سهل
- اعتماد النتائج بضغطة واحدة

### 3. الشفافية
- كل إجراء مسجل
- سجل تاريخي شامل
- معلومات مفصلة

### 4. الكفاءة
- تحديث تلقائي كل 30 ثانية
- بيانات حية
- ترتيب ذكي (الحرج أولاً)

### 5. التصميم الاحترافي
- واجهة واضحة
- ألوان مميزة (أزرق للمزادات)
- تنبيهات بصرية للمزادات الحرجة

---

## الملفات المنفذة

### Frontend
```
src/components/platform/B2BAuctionsOpsRoom.tsx
```

### Backend (Migration)
```
supabase/migrations/create_b2b_operations_room_functions.sql
```

### Routes
```typescript
// في App.tsx
<Route path="/admin/operations-room/b2b" element={<B2BAuctionsOpsRoom />} />
```

---

## الاختبار

```bash
npm run build
# ✓ Build نجح بدون أخطاء
```

---

## التمييز البصري

### B2F (استثمار المزارع)
- لون أخضر (emerald)
- أيقونة ورقة شجر (Leaf)

### B2B (المزادات)
- لون أزرق (blue)
- أيقونة مطرقة المزادات (Gavel)

---

## حالات المزادات

### الحالات المتاحة
1. **active** - نشط
   - المزاد جاري
   - يمكن إيقافه أو تمديده
   - يمكن اعتماد النتيجة

2. **pending** - قيد الانتظار
   - المزاد لم يبدأ بعد

3. **paused** - متوقف
   - المزاد متوقف مؤقتاً
   - يمكن إعادة تفعيله

4. **completed** - مكتمل
   - المزاد انتهى وينتظر المراجعة

5. **sold** - مباع
   - تم اعتماد النتيجة
   - تحديد الفائز

6. **cancelled** - ملغي
   - تم إلغاء المزاد

---

## المزادات الحرجة

**التعريف:** مزاد نشط ينتهي خلال أقل من 24 ساعة

**المؤشرات البصرية:**
- حد أحمر على الجانب الأيمن
- أيقونة تحذير متحركة
- لون أحمر للوقت المتبقي
- يظهر أولاً في القائمة

**الهدف:** لفت انتباه المدير للمزادات التي تحتاج متابعة عاجلة

---

## صيغة عرض الوقت المتبقي

```typescript
أقل من ساعة   → "X دقيقة"
أقل من 24 ساعة → "X ساعة"
24 ساعة وأكثر  → "X يوم"
انتهى          → "انتهى"
```

---

## الفرق بين B2F و B2B

| الميزة | B2F (المزارع) | B2B (المزادات) |
|--------|--------------|----------------|
| اللون | أخضر | أزرق |
| الأيقونة | ورقة شجر | مطرقة مزادات |
| التركيز | حجوزات، زيارات، مدراء | مزايدات، مشاهدات، نتائج |
| Quick Actions | حجوزات، مدير، حالة | إيقاف، تمديد، اعتماد نتيجة |
| الحالات الحرجة | طلبات متأخرة +48 ساعة | مزادات تنتهي خلال 24 ساعة |
| الترتيب | حسب تاريخ الإنشاء | حرج → نشط → باقي |

---

## نصائح الاستخدام

1. راقب المزادات الحرجة أولاً
2. مدد الوقت إذا كانت المزايدات نشطة
3. أوقف المزاد إذا كانت هناك مشكلة
4. اعتمد النتيجة فقط بعد التحقق
5. راجع السجل بانتظام

---

## التطوير المستقبلي

- إضافة إحصائيات متقدمة للمزايدات
- نظام تنبيهات فورية للمزادات الحرجة
- تقارير المزادات المكتملة
- تحليلات الأداء
- دمج مع نظام الدفع

---

## الخلاصة

تم إنشاء غرفة عمليات B2B (المزادات) كاملة مع:
- ✓ Pulse حي (4 أرقام)
- ✓ Radar قائمة المزادات
- ✓ Quick Actions (إيقاف، تمديد، اعتماد)
- ✓ Decision Queue مشترك
- ✓ Executive Log إلزامي مشترك
- ✓ تحديث تلقائي
- ✓ تنبيهات للمزادات الحرجة
- ✓ تصميم احترافي
- ✓ أمان شامل
- ✓ تكامل كامل مع B2F

النظام جاهز للعمل ويوفر تحكم كامل في المزادات بكفاءة وشفافية عالية!
