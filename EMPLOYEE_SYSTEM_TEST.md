# اختبار نظام إضافة الموظفين

## 🎯 هدف الاختبار
التأكد من أن نظام إضافة الموظفين يعمل بشكل صحيح من البداية للنهاية.

---

## ✅ اختبار 1: الوصول للنموذج

### الخطوات:
1. سجل دخول كمدير عام (General Manager)
2. اذهب إلى `/hq`
3. اضغط على تبويب **"إدارة الفريق والصلاحيات"** (التبويب الثالث)
4. يجب أن تظهر 4 بطاقات:
   - إدارة الموظفين (أزرق)
   - نظام الباركود والـ PIN (أخضر)
   - جلسات الإدارة (بنفسجي)
   - سجل الدخول والتدقيق (برتقالي)
5. اضغط على بطاقة **"إدارة الموظفين"**
6. يجب أن تظهر قائمة الموظفين مع زر **"إضافة موظف"**

### النتيجة المتوقعة:
✅ زر "إضافة موظف" موجود ويعمل

---

## ✅ اختبار 2: الخطوة الأولى (البيانات)

### الخطوات:
1. اضغط على زر **"إضافة موظف"**
2. يجب أن يفتح Modal كبير
3. تحقق من وجود:
   - شريط التقدم (الخطوة 1 من 3)
   - 4 حقول إلزامية:
     - الاسم الكامل *
     - رقم الجوال *
     - المسمى الوظيفي *
     - القسم *
   - 1 حقل اختياري:
     - المدير المباشر
4. أدخل البيانات التالية:
   ```
   الاسم: محمد أحمد السعيد
   الجوال: +966501234567
   المسمى: مشرف عمليات
   القسم: استثمار أشجار المزارع (B2F)
   ```
5. اضغط زر **"التالي"**

### النتيجة المتوقعة:
✅ ينتقل للخطوة الثانية
✅ شريط التقدم يُظهر "الخطوة 2 من 3"

---

## ✅ اختبار 3: الخطوة الثانية (الصلاحيات)

### الخطوات:
1. تحقق من وجود 7 قوالب صلاحيات:
   - مدير عام
   - مدير قسم
   - موظف إداري
   - مدير مزرعة
   - مشرف تشغيلي ← **اختر هذا**
   - خدمة مستثمر
   - مالية 2
2. اضغط على **"مشرف تشغيلي"**
3. تحقق من وجود 3 مفاتيح تخصيص:
   - ☑️ إنشاء مهام
   - ☑️ اعتماد مهام/إثباتات
   - ☑️ إرسال تقارير للإدارة العليا
4. فعّل **"إنشاء مهام"** و **"اعتماد مهام/إثباتات"**
5. اضغط زر **"التالي"**

### النتيجة المتوقعة:
✅ ينتقل للخطوة الثالثة
✅ شريط التقدم يُظهر "الخطوة 3 من 3"

---

## ✅ اختبار 4: الخطوة الثالثة (بطاقة الدخول)

### الخطوات:
1. تحقق من الإعدادات الافتراضية:
   - ☑️ تفعيل الدخول بالباركود (مُفعّل)
   - ☑️ يتطلب PIN (مُفعّل)
2. اضغط زر **"توليد تلقائي"** لرمز PIN
3. تحقق من:
   - تم توليد رقم 4 أرقام (مثال: 5847)
4. اقرأ معلومات الجلسة:
   - تبقى فعالة حتى:
     - تسجيل الخروج يدوياً
     - أو 30 دقيقة من عدم النشاط
5. اضغط زر **"إنشاء الموظف"**

### النتيجة المتوقعة:
✅ يظهر رسالة "جاري الإنشاء..."
✅ بعد ثوانٍ تظهر بطاقة الموظف

---

## ✅ اختبار 5: بطاقة الموظف

### التحقق من محتويات البطاقة:

#### يجب أن تحتوي على:
1. **أيقونة دائرية** في الأعلى (خلفية خضراء/تركوازية)
2. **الاسم**: محمد أحمد السعيد
3. **المسمى**: مشرف عمليات
4. **القسم**: استثمار أشجار المزارع
5. **QR Code** (مربع رمادي مع أيقونة QR)
6. **رمز QR Token** أسفل الباركود (مثال: STAFF_1704283920_XYZ123)
7. **الرقم الإداري**: A-00001 (أو رقم تسلسلي آخر)
8. **رمز PIN**: 5847 (أو الرقم المولّد)
9. ✅ **رسالة النجاح**: "تم إنشاء الموظف بنجاح"

#### الأزرار:
- 🖨️ **طباعة البطاقة** (أزرق)
- 💬 **نسخ رسالة واتساب** (أخضر)
- ✅ **إغلاق** (تركوازي - في الأسفل)

### النتيجة المتوقعة:
✅ جميع المعلومات ظاهرة بشكل صحيح

---

## ✅ اختبار 6: نسخ رسالة واتساب

### الخطوات:
1. اضغط زر **"نسخ رسالة واتساب"**
2. يجب أن تظهر رسالة: "تم نسخ الرسالة"
3. افتح أي محرر نصوص والصق (Ctrl+V)

### النتيجة المتوقعة:
```
مرحباً محمد أحمد السعيد،

تم إضافتك كموظف في المنصة

الرقم الإداري: A-00001
المسمى: مشرف عمليات
القسم: استثمار أشجار المزارع

رمز PIN: 5847

يرجى الاحتفاظ ببطاقة الدخول الخاصة بك.
```

✅ الرسالة منسقة ومكتملة

---

## ✅ اختبار 7: التحقق من قاعدة البيانات

### SQL Query 1: التحقق من الموظف
```sql
SELECT
  staff_code,
  full_name,
  phone_number,
  job_title,
  department,
  role,
  qr_code,
  requires_pin,
  pin_code,
  is_active
FROM platform_staff
WHERE phone_number = '+966501234567';
```

### النتيجة المتوقعة:
```
staff_code: A-00001
full_name: محمد أحمد السعيد
phone_number: +966501234567
job_title: مشرف عمليات
department: b2f
role: operations_supervisor
qr_code: STAFF_1704283920_XYZ123
requires_pin: true
pin_code: 5847
is_active: true
```

---

### SQL Query 2: التحقق من الصلاحيات
```sql
SELECT
  permission_template,
  can_create_tasks,
  can_approve_tasks,
  can_send_reports
FROM staff_permissions
WHERE staff_id = (
  SELECT id FROM platform_staff
  WHERE phone_number = '+966501234567'
);
```

### النتيجة المتوقعة:
```
permission_template: operations_supervisor
can_create_tasks: true
can_approve_tasks: true
can_send_reports: false
```

---

### SQL Query 3: التحقق من سجل التدقيق
```sql
SELECT
  action_type,
  target_type,
  target_id,
  details,
  created_at
FROM platform_audit_logs
WHERE action_type = 'staff_created'
ORDER BY created_at DESC
LIMIT 1;
```

### النتيجة المتوقعة:
```
action_type: staff_created
target_type: staff
target_id: <uuid>
details: {
  "staff_name": "محمد أحمد السعيد",
  "department": "b2f",
  "role": "operations_supervisor"
}
created_at: 2026-01-03 12:00:00
```

---

## ✅ اختبار 8: منع التكرار

### الخطوات:
1. اضغط زر **"إضافة موظف"** مرة أخرى
2. أدخل نفس البيانات:
   ```
   الاسم: أي اسم
   الجوال: +966501234567  ← نفس الرقم!
   المسمى: أي مسمى
   القسم: أي قسم
   ```
3. انتقل للخطوة 2 والخطوة 3
4. اضغط **"إنشاء الموظف"**

### النتيجة المتوقعة:
❌ رسالة خطأ: **"رقم الجوال مسجل مسبقاً لموظف آخر"**
✅ لا يتم إنشاء موظف مكرر

---

## ✅ اختبار 9: التحقق من قائمة الموظفين

### الخطوات:
1. اضغط زر **"إغلاق"** في بطاقة الموظف
2. يجب أن ترجع لقائمة الموظفين
3. تحقق من وجود الموظف الجديد في القائمة:
   - الاسم: محمد أحمد السعيد
   - الجوال: +966501234567
   - القسم: استثمار المزارع (شارة خضراء)
   - الحالة: نشط (أيقونة خضراء)
   - الباركود: ✅ باركود مفعّل

### النتيجة المتوقعة:
✅ الموظف الجديد ظاهر في القائمة
✅ جميع البيانات صحيحة

---

## ✅ اختبار 10: البحث والفلترة

### اختبار البحث:
1. في حقل البحث، اكتب: "محمد"
2. يجب أن يظهر الموظف في النتائج
3. امسح البحث، اكتب: "+966501234567"
4. يجب أن يظهر نفس الموظف

### اختبار الفلترة:
1. اختر من قائمة "القسم": **استثمار المزارع (B2F)**
2. يجب أن يظهر فقط موظفي B2F
3. اختر: **كل الأقسام**
4. يجب أن تظهر جميع الموظفين

### النتيجة المتوقعة:
✅ البحث يعمل بشكل صحيح
✅ الفلترة تعمل بشكل صحيح

---

## ✅ اختبار 11: تعطيل الموظف

### الخطوات:
1. ابحث عن الموظف الجديد في القائمة
2. اضغط زر **"تعطيل"** (أحمر)
3. يجب أن تتغير حالة الموظف إلى "معطل"
4. الأيقونة تتحول من خضراء لرمادية
5. اضغط زر **"تفعيل"** (أخضر)
6. يجب أن تعود الحالة إلى "نشط"

### النتيجة المتوقعة:
✅ التفعيل/التعطيل يعمل بشكل صحيح

---

## 📊 ملخص الاختبارات

| # | الاختبار | الحالة |
|---|----------|--------|
| 1 | الوصول للنموذج | ⏳ |
| 2 | الخطوة الأولى (البيانات) | ⏳ |
| 3 | الخطوة الثانية (الصلاحيات) | ⏳ |
| 4 | الخطوة الثالثة (بطاقة الدخول) | ⏳ |
| 5 | بطاقة الموظف | ⏳ |
| 6 | نسخ رسالة واتساب | ⏳ |
| 7 | التحقق من قاعدة البيانات | ⏳ |
| 8 | منع التكرار | ⏳ |
| 9 | قائمة الموظفين | ⏳ |
| 10 | البحث والفلترة | ⏳ |
| 11 | تعطيل/تفعيل الموظف | ⏳ |

**الأسطورة:**
- ⏳ قيد الاختبار
- ✅ نجح
- ❌ فشل

---

## 🔧 استكشاف الأخطاء

### مشكلة: النموذج لا يفتح
**الحل:**
1. تأكد من أنك مسجل دخول كمدير عام
2. تحقق من وجود التبويب "إدارة الفريق والصلاحيات"
3. امسح الـ cache: `Ctrl + Shift + R`

### مشكلة: رسالة خطأ عند الإنشاء
**الحل:**
1. تحقق من اتصال قاعدة البيانات
2. تأكد من تطبيق الـ migration الجديد
3. راجع Console للأخطاء

### مشكلة: البطاقة لا تظهر
**الحل:**
1. تحقق من Console للأخطاء
2. تأكد من نجاح الإنشاء في قاعدة البيانات
3. تحديث الصفحة

---

## ✅ معايير النجاح

النظام يعتبر **ناجحاً** إذا:
- ✅ جميع الخطوات الـ 3 تعمل بسلاسة
- ✅ البيانات تُحفظ في قاعدة البيانات
- ✅ بطاقة الموظف تظهر بشكل صحيح
- ✅ منع التكرار يعمل
- ✅ نسخ رسالة واتساب تعمل
- ✅ قائمة الموظفين تُحدّث تلقائياً
- ✅ البحث والفلترة يعملان
- ✅ سجل التدقيق يُسجل العملية

---

## 📝 ملاحظات الاختبار

استخدم هذا القسم لتسجيل أي ملاحظات أثناء الاختبار:

```
التاريخ: _________________
المختبر: _________________

الملاحظات:
1.
2.
3.

الأخطاء المكتشفة:
1.
2.
3.

التحسينات المقترحة:
1.
2.
3.
```

---

🎯 **ابدأ الاختبار الآن!**
