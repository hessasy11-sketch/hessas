# دليل اختبار المرحلة 2: إثباتات المهام ⚡

## 🎯 الهدف
اختبار نظام رفع ومراجعة إثباتات المهام مع التأكد من ظهور الأحداث في Timeline.

---

## 📍 المسار
```
/admin/b2f/farms/:farmId
└── Tab: مهام التشغيل
```

---

## 🧪 Test Case 1: رفع إثبات (دور العامل)

### الخطوات:

1. **افتح صفحة المزرعة**
   ```
   /admin/b2f/farms/:farmId
   ```

2. **اذهب لـ Tab "مهام التشغيل"**
   - يجب أن ترى قائمة المهام

3. **ابحث عن مهمة "ري القطاع الشمالي"**
   ```
   ✅ الحالة: pending
   ✅ يوجد زر أزرق: "رفع إثبات" 📤
   ```

4. **اضغط على "رفع إثبات"**
   - يفتح Modal

5. **في Modal:**
   ```
   ✅ عنوان: "رفع إثبات إنجاز المهمة"
   ✅ اسم المهمة يظهر
   ✅ منطقة رفع الملف (Upload area)
   ✅ حقل الملاحظات
   ✅ نصائح مفيدة
   ```

6. **ارفع صورة:**
   - اضغط على منطقة الرفع
   - اختر صورة من جهازك
   - يجب أن تظهر معاينة للصورة

7. **أضف ملاحظات:**
   ```
   مثال: "تم الري بنجاح، استهلاك 200 لتر ماء"
   ```

8. **اضغط "رفع الإثبات"**
   ```
   ✅ يظهر "جاري الرفع..."
   ✅ Modal يغلق تلقائياً
   ✅ الصفحة تتحدث
   ```

9. **تحقق من النتائج:**
   ```
   ✅ حالة المهمة تغيرت → "submitted"
   ✅ الزر تغير من "رفع إثبات" إلى حالة أخرى
   ```

10. **افتح Tab "السجل الزمني"**
    ```
    ✅ يجب أن ترى حدث جديد:
       📤 رفع إثبات
       بواسطة: عامل
       المهمة: ري القطاع الشمالي
       منذ: قليل
    ```

---

## 🧪 Test Case 2: مراجعة الإثبات (دور المدير)

### الخطوات:

1. **ارجع لـ Tab "مهام التشغيل"**

2. **ابحث عن مهمة "فحص حالة الأشجار"**
   ```
   ✅ الحالة: submitted
   ✅ يوجد زر كهرماني: "مراجعة الإثبات (X)" 👁️
   ```

3. **اضغط على "مراجعة الإثبات"**
   - يفتح Modal

4. **في Modal:**
   ```
   ✅ Header أخضر: "مراجعة إثبات المهمة"
   ✅ معلومات المكلف
   ✅ تاريخ التسليم
   ✅ ملاحظات العامل (إذا وجدت)
   ✅ معرض الإثباتات
   ✅ زران: [اعتماد] [رفض]
   ```

5. **اضغط على صورة (إن وجدت):**
   ```
   ✅ تفتح Lightbox (معاينة كاملة)
   ✅ زر X للإغلاق
   ```

---

## 🧪 Test Case 3: اعتماد المهمة

### الخطوات:

1. **في Modal المراجعة**

2. **اضغط "اعتماد المهمة" ✅**
   ```
   ✅ يظهر نموذج اعتماد (خلفية خضراء)
   ✅ حقل ملاحظات (اختياري)
   ```

3. **أضف ملاحظات:**
   ```
   مثال: "عمل ممتاز، جميع الأشجار بحالة جيدة"
   ```

4. **اضغط "تأكيد الاعتماد"**
   ```
   ✅ يظهر "جاري الاعتماد..."
   ✅ Modal يغلق
   ✅ الصفحة تتحدث
   ```

5. **تحقق من النتائج:**
   ```
   ✅ حالة المهمة → "approved"
   ✅ يظهر ✅ "تم الاعتماد"
   ```

6. **افتح Tab "السجل الزمني"**
   ```
   ✅ حدث جديد:
      ✅ اعتماد مهمة
      بواسطة: مدير المزرعة
      المهمة: فحص حالة الأشجار
      الملاحظات: "عمل ممتاز..."
      منذ: قليل
   ```

---

## 🧪 Test Case 4: رفض المهمة

### الخطوات:

1. **ارجع لمهمة أخرى بحالة "submitted"**

2. **افتح "مراجعة الإثبات"**

3. **اضغط "رفض المهمة" ❌**
   ```
   ✅ يظهر نموذج رفض (خلفية حمراء)
   ✅ حقل "سبب الرفض" (إجباري)
   ```

4. **اكتب سبب الرفض:**
   ```
   مثال: "الصور غير واضحة، يرجى إعادة الرفع"
   ```

5. **اضغط "تأكيد الرفض"**
   ```
   ✅ يظهر "جاري الرفض..."
   ✅ Modal يغلق
   ✅ الصفحة تتحدث
   ```

6. **تحقق من النتائج:**
   ```
   ✅ حالة المهمة → "rejected"
   ✅ يظهر ⚠️ "مرفوض"
   ```

7. **افتح Tab "السجل الزمني"**
   ```
   ✅ حدث جديد:
      ❌ رفض مهمة
      بواسطة: مدير المزرعة
      المهمة: ...
      السبب: "الصور غير واضحة..."
      منذ: قليل
   ```

---

## 🎯 Checklist النهائي

### وظائف العامل:
- [ ] يرى زر "رفع إثبات" للمهام pending/in_progress
- [ ] يمكنه رفع صورة
- [ ] يمكنه إضافة ملاحظات
- [ ] الرفع يعمل بنجاح
- [ ] الحالة تتغير إلى submitted
- [ ] يظهر حدث في Timeline

### وظائف المدير:
- [ ] يرى زر "مراجعة الإثبات" للمهام submitted
- [ ] العدد يظهر بجانب الزر
- [ ] يمكنه مشاهدة الإثباتات
- [ ] يمكنه قراءة ملاحظات العامل
- [ ] يمكنه اعتماد المهمة مع ملاحظات
- [ ] يمكنه رفض المهمة مع سبب
- [ ] الحالة تتغير (approved/rejected)
- [ ] يظهر حدث في Timeline

### التكامل:
- [ ] جميع الأحداث تسجل في Timeline
- [ ] الوقت النسبي صحيح
- [ ] أسماء الفاعلين تظهر
- [ ] التفاصيل كاملة
- [ ] لا أخطاء في Console

---

## 💡 نصائح للاختبار

### 1. استخدم صور حقيقية
```
- جرب صور مختلفة الأحجام
- جرب ملفات PDF أيضاً
```

### 2. اختبر الحالات الحدية
```
- رفع بدون ملاحظات
- اعتماد بدون ملاحظات
- رفض بدون سبب (يجب أن يمنع)
```

### 3. راقب Console
```
- افتح Developer Tools
- Tab: Console
- تأكد من عدم وجود أخطاء حمراء
```

### 4. اختبر Timeline
```
- تأكد من ظهور كل حدث
- تحقق من الترتيب الزمني
- تأكد من الألوان والأيقونات
```

---

## 🐛 مشاكل محتملة وحلولها

### المشكلة: "لا يوجد زر رفع إثبات"
```
الحل:
- تأكد أن المهمة requires_proof = true
- تأكد أن الحالة pending أو in_progress
```

### المشكلة: "الرفع يفشل"
```
الحل:
1. تحقق من Storage bucket موجود
2. تحقق من RLS policies
3. افتح Console وشاهد الخطأ
```

### المشكلة: "لا يظهر في Timeline"
```
الحل:
1. تحقق أن Function نفذت بنجاح
2. افتح Supabase Dashboard → Table Editor
3. تحقق من farm_activity_timeline
```

### المشكلة: "Modal لا يغلق"
```
الحل:
- Reload الصفحة
- تحقق من Console للأخطاء
```

---

## 📸 Screenshots المطلوبة

للتوثيق، التقط:

1. **زر "رفع إثبات"** في قائمة المهام
2. **ProofUploadModal** مفتوح
3. **معاينة صورة** قبل الرفع
4. **زر "مراجعة الإثبات"** للمهمة submitted
5. **ProofReviewModal** مع الإثباتات
6. **نموذج الاعتماد** (خلفية خضراء)
7. **نموذج الرفض** (خلفية حمراء)
8. **Timeline** يظهر الأحداث الثلاثة:
   - 📤 رفع إثبات
   - ✅ اعتماد مهمة
   - ❌ رفض مهمة

---

**جاهز للاختبار! 🚀**

**المرحلة 2 جاهزة للإطلاق! 🎉**
