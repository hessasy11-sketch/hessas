# دليل ربط QR Codes بإدارة الأقسام

## نظام متكامل للتنظيف التلقائي

تم ربط QR Codes بالكامل مع نظام إدارة الأقسام مع تنظيف تلقائي وصيانة ذكية.

---

## ما تم إضافته ✅

### 1. **Triggers التلقائية**

#### تعطيل/تفعيل QR مع الموظف
```sql
trigger_auto_deactivate_qr
```
- عند تعطيل موظف → QR يُعطّل تلقائياً
- عند تفعيل موظف → QR يُفعّل تلقائياً
- تسجيل كامل في Audit Logs

#### مسح QR عند إزالة القسم
```sql
trigger_auto_clear_qr_on_dept_removal
```
- عند مسح/حذف قسم موظف → QR يُمسح تلقائياً
- لا يمكن لموظف بدون قسم الاحتفاظ بـ QR
- تسجيل السبب في Audit Logs

#### تسجيل حذف QR
```sql
trigger_cascade_delete_qr
```
- عند حذف موظف → يُسجل في Audit Logs
- احتفاظ بسجل كامل للـ QR المحذوفة
- للتتبع والمراجعة

### 2. **دالات الصيانة**

#### التنظيف التلقائي
```sql
cleanup_orphaned_qr_codes()
```
**يقوم بـ:**
- تعطيل QR للموظفين المعطّلين
- مسح QR للموظفين بدون قسم
- تسجيل جميع العمليات

**الاستخدام:**
```sql
SELECT cleanup_orphaned_qr_codes();
```

**النتيجة:**
```json
{
  "success": true,
  "deactivated_qr": 3,
  "cleared_qr": 2,
  "message": "تم تنظيف 5 QR Code"
}
```

#### المزامنة التلقائية
```sql
sync_qr_with_staff_status()
```
**يقوم بـ:**
- مزامنة حالة QR مع حالة الموظف
- إصلاح أي عدم تطابق
- ضمان التوافق الكامل

#### تقرير الصحة
```sql
get_qr_cleanup_report()
```
**يُرجع:**
```json
{
  "total_staff": 23,
  "active_qr": 20,
  "inactive_staff_with_qr": 2,
  "no_department_with_qr": 1,
  "orphaned_qr": 3,
  "need_cleanup": 3,
  "cleanup_needed": true,
  "health_status": "warning"
}
```

**health_status المحتملة:**
- `excellent` - لا توجد مشاكل
- `good` - مشاكل بسيطة (< 5)
- `warning` - يحتاج انتباه (5-10)
- `critical` - حرج (> 10)

#### جلب QR اليتيمة
```sql
get_orphaned_qr_codes()
```
يُرجع قائمة بجميع QR التي:
- لموظفين معطّلين
- لموظفين بدون قسم
- تحتاج تنظيف

#### حذف موظف مع تنظيف كامل
```sql
delete_staff_with_cleanup(staff_id)
```
**يقوم بـ:**
- حذف من التعيينات
- حذف من Permission Packs
- حذف الموظف
- تسجيل QR في Audit
- تنظيف شامل

---

## واجهة المستخدم 🎨

### الوصول
```
Work Management Hub → بطاقات الموظفين → مسح وفحص QR
```

### الأقسام الجديدة

#### 1. لوحة الإحصائيات (6 بطاقات)
- إجمالي الموظفين
- QR نشط
- QR معطّل
- QR مكرر
- بدون QR
- نسبة الصحة

#### 2. ماسح QR
- مسح فوري من الكاميرا
- تحليل شامل
- عرض المشاكل

#### 3. QR المكررة
- قائمة التكرارات
- إصلاح تلقائي

#### 4. **الصيانة التلقائية** ⭐ جديد
**إحصائيات:**
- موظفين معطّلين بـ QR نشط
- موظفين بدون قسم ولديهم QR
- QR يتيمة (orphaned)
- حالة النظام (ممتاز/جيد/تحذير/حرج)

**أدوات:**
- زر "مزامنة" → يزامن حالة QR مع الموظفين
- زر "تنظيف تلقائي" → ينظف QR اليتيمة

**قائمة QR تحتاج صيانة:**
- عرض جميع QR المشكلة
- حالة كل موظف
- سبب المشكلة (معطّل/بلا قسم)

---

## سيناريوهات الاستخدام 📋

### سيناريو 1: تعطيل موظف
**الإجراء:**
```
مشرف → إدارة الأقسام → تعطيل موظف
```

**ما يحدث تلقائياً:**
1. ✅ QR يُعطّل فوراً
2. ✅ يُسجل في Audit Logs
3. ✅ لا يمكن استخدام QR للدخول
4. ✅ يظهر في قائمة "QR تحتاج صيانة"

### سيناريو 2: إزالة قسم موظف
**الإجراء:**
```
مشرف → إدارة الأقسام → مسح القسم
```

**ما يحدث تلقائياً:**
1. ✅ QR يُمسح كلياً
2. ✅ يُسجل في Audit Logs
3. ✅ لا يمكن الدخول
4. ✅ يجب إعادة تعيين قسم وتوليد QR جديد

### سيناريو 3: حذف موظف
**الإجراء:**
```sql
SELECT delete_staff_with_cleanup('staff_id');
```

**ما يحدث تلقائياً:**
1. ✅ حذف من جميع التعيينات
2. ✅ حذف من Permission Packs
3. ✅ تسجيل QR في Audit
4. ✅ حذف الموظف نهائياً
5. ✅ تنظيف كامل للبيانات المرتبطة

### سيناريو 4: تنظيف دوري
**الإجراء:**
```
Work Management → بطاقات الموظفين → مسح وفحص QR → تنظيف تلقائي
```

**ما يحدث:**
1. ✅ تعطيل QR للموظفين المعطّلين
2. ✅ مسح QR للموظفين بدون قسم
3. ✅ تسجيل جميع العمليات
4. ✅ تحديث الإحصائيات

---

## الأمان والتدقيق 🔒

### تسجيل كامل في Audit Logs

كل عملية تُسجل في `platform_audit_logs`:

#### أنواع الأحداث الجديدة:
```sql
'auto_activate_qr'      -- تفعيل تلقائي
'auto_deactivate_qr'    -- تعطيل تلقائي
'auto_clear_qr'         -- مسح تلقائي
'cascade_delete_qr'     -- حذف cascade
'cleanup_qr'            -- تنظيف
'sync_qr'               -- مزامنة
```

#### مثال سجل:
```json
{
  "action_type": "auto_clear_qr",
  "target_type": "platform_staff",
  "target_id": "uuid-here",
  "metadata": {
    "staff_name": "محمد أحمد",
    "staff_code": "EMP-001",
    "old_department": "المبيعات",
    "reason": "department_removed"
  }
}
```

### تتبع كامل
- **من** قام بالفعل
- **ماذا** حدث
- **متى** حدث
- **لماذا** حدث
- **التفاصيل** الكاملة

---

## أفضل الممارسات 💡

### 1. تنظيف دوري
```
يومياً → راجع لوحة الصيانة
أسبوعياً → شغّل التنظيف التلقائي
شهرياً → راجع Audit Logs
```

### 2. مراقبة الصحة
```
Health Score < 90% → تنظيف فوري
Need Cleanup > 5 → مراجعة السياسات
Orphaned QR > 10 → تحقيق شامل
```

### 3. إدارة الأقسام
- لا تحذف قسم موظف بدون سبب
- عند نقل موظف، غيّر القسم مباشرة
- لا تترك موظفين بدون قسم
- QR للموظفين النشطين فقط

### 4. صيانة وقائية
```sql
-- تشغيل يومي (cron job)
SELECT cleanup_orphaned_qr_codes();
SELECT sync_qr_with_staff_status();
```

---

## الأسئلة الشائعة ❓

### هل يتم حذف QR نهائياً عند حذف موظف؟
نعم، لكن يتم تسجيله في Audit Logs للرجوع إليه.

### ماذا يحدث إذا أعدت تفعيل موظف معطّل؟
QR يُفعّل تلقائياً إذا كان موجوداً.

### هل يمكن لموظف بدون قسم استخدام QR؟
لا، سيُمسح QR تلقائياً عند إزالة القسم.

### كم مرة يجب تشغيل التنظيف؟
مرة يومياً كافية، أو عند ظهور تحذير في لوحة الصيانة.

### هل التنظيف آمن؟
نعم 100%، جميع العمليات مُسجلة ولا يتم حذف بيانات دائمة إلا QR.

---

## الصيانة الدورية 🔧

### يومي
```sql
SELECT get_qr_cleanup_report();
-- إذا cleanup_needed = true
SELECT cleanup_orphaned_qr_codes();
```

### أسبوعي
```sql
SELECT sync_qr_with_staff_status();
SELECT find_duplicate_qr_codes();
SELECT fix_duplicate_qr_codes(); -- إذا وُجدت
```

### شهري
```sql
-- مراجعة Audit Logs
SELECT * FROM platform_audit_logs
WHERE action_type LIKE '%qr%'
AND created_at > now() - interval '30 days'
ORDER BY created_at DESC;
```

---

## تقرير الحالة 📊

### الفحص السريع
```sql
-- صحة النظام
SELECT get_qr_cleanup_report();

-- QR مكررة
SELECT * FROM find_duplicate_qr_codes();

-- QR يتيمة
SELECT * FROM get_orphaned_qr_codes();

-- الإحصائيات
SELECT get_qr_codes_statistics();
```

### النتيجة المثالية:
```json
{
  "need_cleanup": 0,
  "cleanup_needed": false,
  "health_status": "excellent",
  "duplicate_qr": 0,
  "orphaned_qr": 0
}
```

---

## الخلاصة ✨

### ما تم تحقيقه:
✅ **ربط كامل** مع إدارة الأقسام
✅ **تنظيف تلقائي** للـ QR اليتيمة
✅ **مزامنة ذكية** للحالات
✅ **تسجيل شامل** في Audit Logs
✅ **واجهة سهلة** للصيانة
✅ **أمان 100%** بدون فقدان بيانات

### الفوائد:
- 🎯 لا QR مكررة
- 🧹 لا QR يتيمة
- 🔄 مزامنة تلقائية
- 📝 تتبع كامل
- 🚀 صيانة سهلة
- ✅ نظام نظيف دائماً

### التوصية:
راجع لوحة الصيانة يومياً، وشغّل التنظيف عند الحاجة!
