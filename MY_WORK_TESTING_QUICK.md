# 🧪 صفحة "عملي اليوم" - اختبار سريع
## My Work Page - Quick Testing Guide

---

## ⚡ اختبار 3 دقائق

### الخطوة 1: تسجيل الدخول كموظف عادي

```bash
1. افتح /admin/gateway
2. سجل دخول كـ b2f_assistant (أو أي موظف عادي)
3. تحقق: تم التوجيه التلقائي إلى /admin/my-work ✅
```

---

### الخطوة 2: تحقق من الأقسام الظاهرة

```bash
✅ قسم الإحصائيات (3 بطاقات):
   - المهام المفتوحة: X
   - تنبيهات عاجلة: Y
   - (لا يظهر "اعتمادات تنتظرني" لأنه موظف عادي)

✅ قسم "مهامي الآن" مع تبويبات:
   - [مفتوحة (X)] [بانتظار الاعتماد (Y)] [مكتملة (Z)]

✅ قسم "تنبيهات عملي"
```

**النتيجة:** إذا ظهرت الأقسام بهذا الشكل = ✅ نجاح

---

### الخطوة 3: اختبر أزرار العمل

```bash
1. إذا لديك مهمة في حالة "pending":
   ✅ يظهر زر "بدء"
   ✅ اضغط عليه
   ✅ تحقق: تحولت إلى "in_progress"
   ✅ تحقق: زر "تم" ظهر

2. اضغط على زر "تم":
   ✅ تحقق: تحولت إلى "بانتظار الاعتماد"
   ✅ تحقق: المهمة انتقلت للتبويب الثاني
```

**النتيجة:** إذا الأزرار تعمل = ✅ نجاح

---

### الخطوة 4: اختبر مهام المزارع

```bash
إذا لديك مهمة من farm_tasks:
   ✅ يظهر badge أخضر: "🌱 مزرعة"
   ✅ يظهر اسم المزرعة: "📍 مزرعة الأمل"
   ✅ الأزرار تعمل بنفس الطريقة
```

**النتيجة:** إذا badge يظهر = ✅ نجاح

---

### الخطوة 5: اختبر زر "بوابة الإدارة"

```bash
1. اضغط على الزر الذهبي في أعلى اليمين
2. تحقق: رجعت إلى /admin/gateway ✅
```

**النتيجة:** إذا الزر يعمل = ✅ نجاح

---

## 🔍 اختبار المدير (دقيقة إضافية)

### الخطوة 1: تسجيل الدخول كمدير

```bash
1. سجل خروج
2. سجل دخول كـ supervisor أو farm_manager
3. انتقل تلقائياً إلى /admin/my-work
```

---

### الخطوة 2: تحقق من قسم الاعتمادات

```bash
✅ قسم "اعتمادات تنتظرني" يظهر الآن
✅ إذا لديك مصروفات أو قرارات معلقة:
   - يظهر العدد
   - يظهر المبلغ (للمصروفات)
   - يظهر "من: اسم الطالب"
   - زر "راجع الآن" موجود

✅ اضغط على "راجع الآن":
   - مصروف → يذهب إلى /admin/operations-room/b2f
   - قرار → يذهب إلى /admin/operations-room/global
```

**النتيجة:** إذا القسم يظهر للمدير = ✅ نجاح

---

## 📊 النتائج السريعة

| الاختبار | النتيجة |
|----------|---------|
| الصفحة تفتح بدون أخطاء | ✅ / ❌ |
| الأقسام الثلاثة تظهر | ✅ / ❌ |
| التبويبات تعمل | ✅ / ❌ |
| أزرار "بدء" و "تم" تعمل | ✅ / ❌ |
| badge المزرعة يظهر | ✅ / ❌ |
| قسم الاعتمادات (للمدير) | ✅ / ❌ |
| زر "بوابة الإدارة" يعمل | ✅ / ❌ |

---

## 🐛 مشاكل محتملة وحلول

### المشكلة 1: "لا يظهر قسم الاعتمادات"

**السبب:** الدور ليس ضمن approval roles

**الحل:**
```javascript
// تحقق من الدور:
const approvalRoles = [
  'general_manager',
  'supervisor',
  'manager',
  'farm_manager',
  'accountant'
];

// إذا الدور غير موجود، أضفه
```

---

### المشكلة 2: "المهام لا تظهر"

**السبب:** staffId غير صحيح

**الحل:**
```javascript
// افتح Console (F12)
const session = localStorage.getItem('staff_session');
console.log('Session:', JSON.parse(session));

// تحقق من staffId
// ثم تحقق من DB:
SELECT * FROM staff_tasks WHERE assigned_to = 'staff-id';
SELECT * FROM farm_tasks WHERE assigned_to_user_id = 'staff-id';
```

---

### المشكلة 3: "زر بدء/تم لا يعمل"

**السبب:** خطأ في التحديث

**الحل:**
```javascript
// افتح Console (F12)
// يجب أن ترى:
Error updating task: ...

// تحقق من الـ error
// غالباً RLS policy مشكلة
```

---

### المشكلة 4: "badge المزرعة لا يظهر"

**السبب:** source ليس 'farm'

**الحل:**
```javascript
// تحقق من البيانات في Console:
console.log('Tasks:', tasks);

// يجب أن يكون:
{ source: 'farm', farm_name: 'xxx' }

// إذا source = 'staff'، المهمة ليست من المزرعة
```

---

## ✅ قائمة تحقق سريعة

قبل إنهاء الاختبار:

- [ ] الصفحة تفتح بدون أخطاء
- [ ] الأقسام الثلاثة (أو أربعة للمدير) تظهر
- [ ] التبويبات تنقل بين الحالات
- [ ] أزرار "بدء" و "تم" تحدث الحالة
- [ ] badge المزرعة يظهر للمهام المناسبة
- [ ] قسم الاعتمادات يظهر للمدير فقط
- [ ] زر "بوابة الإدارة" يرجع للبوابة

---

## 🚀 النتيجة النهائية

إذا نجحت جميع الاختبارات:

```
✅ صفحة "عملي اليوم" نشطة وجاهزة
✅ المهام تُجمع من staff_tasks + farm_tasks
✅ التبويبات تعمل بشكل صحيح
✅ الأزرار تحدث الحالات
✅ قسم الاعتمادات للمدير فقط
✅ التنبيهات تظهر حسب الأولوية

النظام جاهز للإنتاج! 🚀
```

---

**وقت الاختبار:** 3-5 دقائق
**الصعوبة:** سهل
**الأهمية:** عالية جداً

ابدأ الاختبار الآن! 🧪
