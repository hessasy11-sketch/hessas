# دليل لوحة الصلاحيات الإدارية (Authority Panel) - المرحلة E

## نظرة عامة

تم إنشاء نظام شامل لإدارة الصلاحيات الإدارية العليا (Authority Panel) يسمح للمدير العام فقط بإدارة 5 أدوار رئيسية في المنصة.

---

## الموقع

### في غرفة عمليات B2F
```
/admin/operations-room/b2f
→ زر "الصلاحيات" في الهيدر (لون ذهبي)
```

### في غرفة عمليات B2B
```
/admin/operations-room/b2b
→ زر "الصلاحيات" في الهيدر (لون ذهبي)
```

---

## الأدوار الخمسة

### 1. مساعد B2F
```
الدور: b2f_assistant
الأيقونة: ورقة شجر (Leaf)
اللون: أخضر (emerald)
المسؤوليات: مساعدة في إدارة استثمار المزارع
```

### 2. مدير المزارع الوطني
```
الدور: national_farms_manager
الأيقونة: تاج (Crown)
اللون: بنفسجي (purple)
المسؤوليات: إشراف عام على جميع المزارع
```

### 3. مساعد B2B
```
الدور: b2b_assistant
الأيقونة: مطرقة المزادات (Gavel)
اللون: أزرق (blue)
المسؤوليات: مساعدة في إدارة المزادات
```

### 4. المحاسب
```
الدور: accountant
الأيقونة: آلة حاسبة (Calculator)
اللون: عنبري (amber)
المسؤوليات: إدارة الحسابات والمالية
```

### 5. مدير التسويق
```
الدور: marketing_manager
الأيقونة: اتجاه صاعد (TrendingUp)
اللون: وردي (pink)
المسؤوليات: إدارة التسويق والعلاقات
```

---

## قاعدة البيانات

### الجدول الرئيسي: authority_assignments

```sql
authority_assignments (
  id uuid                    -- معرف التعيين
  staff_id uuid              -- معرف الموظف
  authority_role text        -- الدور (أحد الخمسة)
  assigned_by uuid           -- من قام بالتعيين
  assigned_at timestamptz    -- تاريخ التعيين
  is_active boolean          -- التعيين نشط؟
  is_suspended boolean       -- معلق؟
  is_temporary boolean       -- مؤقت؟
  temporary_until timestamptz -- تاريخ انتهاء الصلاحية المؤقتة
  suspension_reason text     -- سبب التعليق
  suspension_at timestamptz  -- تاريخ التعليق
  suspended_by uuid          -- من قام بالتعليق
  notes text                 -- ملاحظات
  created_at timestamptz     -- تاريخ الإنشاء
  updated_at timestamptz     -- تاريخ آخر تحديث
)
```

**القيود (Constraints):**
```sql
-- يمكن أن يكون للموظف دور واحد فقط من نفس النوع
UNIQUE(staff_id, authority_role)

-- الأدوار المسموحة فقط
CHECK (authority_role IN (
  'b2f_assistant',
  'national_farms_manager',
  'b2b_assistant',
  'accountant',
  'marketing_manager'
))
```

---

## دوال RPC

### 1. get_current_authorities()
**الوظيفة:** عرض جميع التعيينات النشطة

**الإرجاع:**
```json
[
  {
    "id": "uuid",
    "staff_id": "uuid",
    "staff_code": "EMP001",
    "staff_name": "اسم الموظف",
    "authority_role": "b2f_assistant",
    "is_active": true,
    "is_suspended": false,
    "is_temporary": false,
    "temporary_until": null,
    "assigned_at": "2024-01-01T00:00:00Z",
    "assigned_by": "uuid",
    "suspension_reason": null,
    "notes": "ملاحظات"
  }
]
```

**الترتيب:**
```
1. b2f_assistant
2. national_farms_manager
3. b2b_assistant
4. accountant
5. marketing_manager
```

---

### 2. exec_assign_authority()
**الوظيفة:** تعيين موظف في دور إداري

**المعاملات:**
```typescript
{
  p_staff_id: uuid              // معرف الموظف
  p_authority_role: text        // الدور
  p_assigned_by: uuid           // من قام بالتعيين
  p_is_temporary: boolean       // صلاحية مؤقتة؟ (افتراضي: false)
  p_temporary_days: integer     // عدد الأيام للصلاحية المؤقتة
  p_notes: text                 // ملاحظات (اختياري)
}
```

**الإرجاع:**
```json
{
  "success": true,
  "assignment_id": "uuid",
  "log_id": "uuid",
  "temporary_until": "2024-02-01T00:00:00Z"
}
```

**الإجراءات التلقائية:**
- إلغاء أي تعيين سابق في نفس الدور
- حساب تاريخ انتهاء الصلاحية المؤقتة
- التسجيل في Executive Log

---

### 3. exec_revoke_authority()
**الوظيفة:** سحب صلاحية

**المعاملات:**
```typescript
{
  p_assignment_id: uuid         // معرف التعيين
  p_revoked_by: uuid           // من قام بالسحب
  p_notes: text                // ملاحظات (اختياري)
}
```

**الإرجاع:**
```json
{
  "success": true,
  "log_id": "uuid"
}
```

**الإجراءات:**
- تغيير is_active إلى false
- التسجيل في Executive Log

---

### 4. exec_suspend_authority()
**الوظيفة:** تعليق صلاحية مؤقتاً

**المعاملات:**
```typescript
{
  p_assignment_id: uuid         // معرف التعيين
  p_suspended_by: uuid         // من قام بالتعليق
  p_suspension_reason: text    // سبب التعليق (إلزامي)
  p_notes: text                // ملاحظات (اختياري)
}
```

**الإرجاع:**
```json
{
  "success": true,
  "log_id": "uuid"
}
```

**الإجراءات:**
- تغيير is_suspended إلى true
- حفظ سبب التعليق
- تسجيل تاريخ التعليق
- التسجيل في Executive Log

---

### 5. exec_unsuspend_authority()
**الوظيفة:** إلغاء تعليق صلاحية

**المعاملات:**
```typescript
{
  p_assignment_id: uuid         // معرف التعيين
  p_unsuspended_by: uuid       // من قام بإلغاء التعليق
  p_notes: text                // ملاحظات (اختياري)
}
```

**الإرجاع:**
```json
{
  "success": true,
  "log_id": "uuid"
}
```

**الإجراءات:**
- تغيير is_suspended إلى false
- مسح سبب التعليق
- التسجيل في Executive Log

---

### 6. get_available_staff_for_authority()
**الوظيفة:** عرض الموظفين المتاحين للتعيين

**المرشحات:**
```sql
WHERE is_active = true
  AND role IN ('manager', 'supervisor', 'specialist', 'admin')
```

**الإرجاع:**
```json
[
  {
    "id": "uuid",
    "staff_code": "EMP001",
    "name": "اسم الموظف",
    "role": "manager",
    "department": "القسم"
  }
]
```

---

## واجهة Authority Panel

### المظهر العام

**الموقع:** Modal منبثق (Overlay)

**الأبعاد:**
```
العرض: max-w-5xl
الارتفاع: max-h-90vh
الخلفية: أبيض
الحد: rounded-2xl
الظل: shadow-2xl
```

**الهيدر:**
```
الخلفية: gradient من أسود إلى رمادي
الأيقونة: Shield بلون ذهبي
العنوان: "لوحة الصلاحيات الإدارية"
الوصف: "Authority Panel - إدارة GM فقط"
```

---

### عرض الأدوار

كل دور يُعرض في بطاقة (Card) تحتوي على:

#### البطاقة الفارغة (بدون تعيين)
```
→ أيقونة الدور ملونة
→ اسم الدور بالعربي
→ نص "لا يوجد تعيين حالياً"
→ زر "تعيين" باللون الأزرق
```

#### البطاقة المعينة (مع موظف)
```
→ أيقونة الدور ملونة
→ اسم الدور بالعربي
→ شارات (Badges):
   - "معلق" (أحمر) إذا كان معلقاً
   - "مؤقت" (عنبري) إذا كان مؤقتاً
→ معلومات الموظف:
   - الاسم
   - الكود
   - تاريخ التعيين
   - تاريخ الانتهاء (للمؤقت)
   - سبب التعليق (للمعلق)
→ أزرار الإجراءات:
   - "تعليق" (عنبري) - للنشط
   - "إلغاء التعليق" (أخضر) - للمعلق
   - "سحب" (أحمر) - للجميع
```

---

### Modal التعيين

يظهر عند الضغط على زر "تعيين":

**المحتويات:**
```
1. الهيدر:
   - أيقونة الدور ملونة
   - نص "تعيين [اسم الدور]"

2. اختيار الموظف:
   - قائمة منسدلة (Dropdown)
   - تعرض: الاسم - الكود (الدور)
   - مرتبة أبجدياً

3. خيار الصلاحية المؤقتة:
   - Checkbox: "صلاحية مؤقتة"
   - إذا تم التفعيل:
     - حقل "عدد الأيام" (1-365)
     - نص توضيحي: "ستنتهي الصلاحية تلقائياً بعد X يوم"

4. الأزرار:
   - "تعيين الآن" (أزرق)
   - "إلغاء" (رمادي)
```

---

## الإجراءات (Actions)

### 1. تعيين مسؤول

**الخطوات:**
```
1. اضغط على "تعيين" في بطاقة الدور
2. اختر الموظف من القائمة
3. (اختياري) فعل "صلاحية مؤقتة" وحدد الأيام
4. اضغط "تعيين الآن"
```

**النتيجة:**
- يُعين الموظف في الدور
- يظهر اسمه في البطاقة
- يُسجل في Executive Log
- إلغاء أي تعيين سابق

---

### 2. سحب صلاحية

**الخطوات:**
```
1. في بطاقة الدور المعين
2. اضغط زر "سحب" (أحمر)
3. تأكيد الإجراء
```

**النتيجة:**
- إلغاء التعيين (is_active = false)
- البطاقة تصبح فارغة
- يُسجل في Executive Log

---

### 3. تعليق صلاحية

**الخطوات:**
```
1. في بطاقة الدور المعين النشط
2. اضغط زر "تعليق" (عنبري)
3. أدخل سبب التعليق
4. تأكيد
```

**النتيجة:**
- التعيين يصبح معلقاً (is_suspended = true)
- شارة "معلق" تظهر
- سبب التعليق يظهر في البطاقة
- يُسجل في Executive Log

**التأثير:** الموظف لا يستطيع استخدام صلاحياته

---

### 4. إلغاء تعليق

**الخطوات:**
```
1. في بطاقة الدور المعلق
2. اضغط زر "إلغاء التعليق" (أخضر)
```

**النتيجة:**
- التعيين يعود للحالة النشطة
- شارة "معلق" تختفي
- يُسجل في Executive Log

**التأثير:** الموظف يستعيد صلاحياته

---

### 5. منح صلاحية مؤقتة

**الخطوات:**
```
1. عند التعيين، فعل خيار "صلاحية مؤقتة"
2. حدد عدد الأيام (1-365)
3. أكمل التعيين
```

**النتيجة:**
- يُعين الموظف بصلاحية محدودة المدة
- شارة "مؤقت" تظهر
- تاريخ الانتهاء يظهر
- يُسجل في Executive Log

**بعد انتهاء المدة:**
- الصلاحية تنتهي تلقائياً
- (يحتاج cron job أو trigger لتنفيذ ذلك)

---

## التسجيل في Executive Log

جميع الإجراءات تُسجل تلقائياً في executive_logs:

### أنواع الإجراءات المسجلة

```typescript
authority_assigned      // تعيين صلاحية
authority_revoked       // سحب صلاحية
authority_suspended     // تعليق صلاحية
authority_unsuspended   // إلغاء تعليق
```

### بيانات السجل

```json
{
  "id": "uuid",
  "action_type": "authority_assigned",
  "action_data": {
    "staff_id": "uuid",
    "staff_name": "اسم الموظف",
    "authority_role": "b2f_assistant",
    "is_temporary": true,
    "temporary_until": "2024-02-01T00:00:00Z"
  },
  "performed_by": "uuid",
  "result": "success",
  "notes": "تعيين مساعد B2F",
  "created_at": "2024-01-01T00:00:00Z"
}
```

---

## الأمان

### RLS Policies

```sql
-- Service role: صلاحيات كاملة
CREATE POLICY "Service role full access"
  ON authority_assignments
  FOR ALL USING (true);

-- Authenticated users: قراءة فقط
CREATE POLICY "Authenticated read"
  ON authority_assignments
  FOR SELECT TO authenticated USING (true);
```

### صلاحيات التنفيذ

```sql
-- دوال القراءة: للجميع
GRANT EXECUTE ON FUNCTION get_current_authorities
  TO anon, authenticated;

GRANT EXECUTE ON FUNCTION get_available_staff_for_authority
  TO anon, authenticated;

-- دوال التنفيذ: للمصادق عليهم فقط
GRANT EXECUTE ON FUNCTION exec_assign_authority
  TO authenticated;

GRANT EXECUTE ON FUNCTION exec_revoke_authority
  TO authenticated;

GRANT EXECUTE ON FUNCTION exec_suspend_authority
  TO authenticated;

GRANT EXECUTE ON FUNCTION exec_unsuspend_authority
  TO authenticated;
```

### الحماية على مستوى التطبيق

**مطلوب:** تحقق إضافي في Frontend أن المستخدم هو GM فعلاً قبل عرض اللوحة

---

## حالات الاستخدام

### سيناريو 1: تعيين مساعد B2F

```
1. GM يفتح غرفة B2F
2. يضغط على زر "الصلاحيات" في الهيدر
3. لوحة الصلاحيات تفتح
4. يجد بطاقة "مساعد B2F" فارغة
5. يضغط زر "تعيين"
6. يختار موظف من القائمة
7. يضغط "تعيين الآن"
8. البطاقة تظهر اسم الموظف
9. يُسجل الإجراء في Executive Log
```

---

### سيناريو 2: تعليق المحاسب مؤقتاً

```
1. GM يفتح لوحة الصلاحيات
2. يجد بطاقة "المحاسب" معينة
3. يضغط زر "تعليق"
4. نافذة تطلب سبب التعليق
5. يدخل: "في إجازة لمدة أسبوع"
6. يضغط OK
7. شارة "معلق" تظهر
8. السبب يظهر في البطاقة
9. يُسجل في Executive Log
```

---

### سيناريو 3: صلاحية مؤقتة لمدير تسويق

```
1. GM يريد تعيين مدير تسويق بديل لمدة 30 يوم
2. يفتح لوحة الصلاحيات
3. يضغط "تعيين" في بطاقة مدير التسويق
4. يختار الموظف البديل
5. يفعل خيار "صلاحية مؤقتة"
6. يدخل: 30 يوم
7. يضغط "تعيين الآن"
8. البطاقة تظهر:
   - اسم البديل
   - شارة "مؤقت"
   - "ينتهي: [تاريخ بعد 30 يوم]"
9. بعد 30 يوم: الصلاحية تنتهي تلقائياً
```

---

## المزايا

### 1. التحكم المركزي
- إدارة جميع الأدوار من مكان واحد
- رؤية واضحة لجميع التعيينات
- سهولة التبديل بين الأدوار

### 2. المرونة
- صلاحيات مؤقتة لحالات الاستبدال
- تعليق مؤقت بدلاً من السحب الكامل
- ملاحظات لكل إجراء

### 3. الشفافية
- تسجيل إلزامي لكل إجراء
- سجل تاريخي شامل
- أسباب واضحة للتعليق

### 4. الأمان
- RLS محكمة
- صلاحيات واضحة
- تدقيق كامل

### 5. التصميم
- واجهة واضحة وسهلة
- ألوان مميزة لكل دور
- مؤشرات بصرية للحالات

---

## الإحصائيات

```
عدد الأدوار: 5
عدد الدوال: 6
عدد الجداول: 1
عدد الـ RLS Policies: 2
عدد أنواع الإجراءات: 4
عدد أسطر الكود: ~350 (Frontend) + ~400 (Backend)
```

---

## الملفات المنفذة

### Frontend
```
src/components/platform/AuthorityPanel.tsx
```

### Backend
```
supabase/migrations/create_authority_panel_system.sql
```

### التحديثات
```
src/components/platform/B2FOperationsRoom.tsx
src/components/platform/B2BAuctionsOpsRoom.tsx
```

---

## الاختبار

```bash
npm run build
# ✓ Build نجح بدون أخطاء
```

---

## التطوير المستقبلي

### ميزات مقترحة

1. **انتهاء تلقائي للصلاحيات المؤقتة**
   - Cron job يومي
   - إشعارات قبل الانتهاء

2. **تاريخ التعيينات**
   - عرض جميع التعيينات السابقة
   - إحصائيات لكل دور

3. **إشعارات للموظفين**
   - عند التعيين
   - عند التعليق
   - عند السحب

4. **تفويض مؤقت**
   - تفويض صلاحيات لموظف آخر
   - مع احتفاظ الأصلي بصلاحياته

5. **صلاحيات متعددة**
   - موظف واحد لأكثر من دور
   - إدارة الأولويات

---

## نصائح الاستخدام

1. استخدم الصلاحيات المؤقتة للبدائل
2. وثق سبب التعليق دائماً
3. راجع السجل التنفيذي بانتظام
4. تحقق من تواريخ انتهاء الصلاحيات المؤقتة
5. لا تسحب الصلاحية إلا عند الضرورة القصوى

---

## الأسئلة الشائعة

### هل يمكن لموظف أن يكون له أكثر من دور؟
حالياً: لا، كل موظف يمكن أن يكون له دور واحد فقط من كل نوع.
مستقبلاً: يمكن تطوير النظام ليدعم أدوار متعددة.

### ماذا يحدث للصلاحيات المؤقتة بعد انتهائها؟
حالياً: تبقى في الجدول ولكن غير نشطة.
مستقبلاً: يمكن إضافة cron job لإلغائها تلقائياً.

### هل يمكن لمساعد B2F أن يدير لوحة الصلاحيات؟
لا، لوحة الصلاحيات للمدير العام فقط (GM).

### كيف يتم التحقق من صلاحيات الموظف؟
يتم الاستعلام من جدول authority_assignments والتحقق من:
- is_active = true
- is_suspended = false
- (إذا مؤقت) temporary_until > now()

---

## الخلاصة

تم إنشاء نظام شامل لإدارة الصلاحيات الإدارية يوفر:

✓ إدارة 5 أدوار رئيسية
✓ تعيين/سحب فوري
✓ تعليق/إلغاء تعليق
✓ صلاحيات مؤقتة
✓ تسجيل إلزامي
✓ واجهة سهلة وواضحة
✓ أمان محكم
✓ شفافية كاملة

النظام جاهز للعمل ومتكامل مع غرفتي العمليات B2F و B2B!
